# The 116 sections, and where each one stands

The owner's directive — *DJMANZO — ADAPTIVE PROFESSIONAL DJ COCKPIT* — is 105
numbered sections, and the owner has asked for eleven more since (§106–§116), quoted as written at the end of `DIRECTIVE.md`. [`GUI-OVERHAUL.md`](GUI-OVERHAUL.md) is the analysis §99
asked for and turns them into nine phases; this file is the other view, section
by section, so "where are we" is answerable from a file rather than from
somebody's memory.

**It is written that way for a reason.** This question was asked once when the
directive's text was no longer in context, and the honest answer at the time
was "I can only defend twelve of them". A status that lives in a file does not
have that failure mode.

## How to read the marks

- ✅ **done** — shipped and, where it is testable, tested.
- 🟡 **part** — some of it is real; the rest is named in the row.
- ⬜ **open** — not started.
- ⚖️ **standing rule** — a constraint rather than a deliverable. It is honoured
  or it is not; it never becomes "done" because it never stops applying.

Counting rules and rounding are at the bottom, so the numbers can be checked
rather than taken.

## The table

| § | Section | | Where it stands |
|---|---|---|---|
| 1 | Understand the actual repository | ✅ | 37 components, 17,614 lines, 148 commands, 448 parameters — counted, not remembered |
| 2 | Treat the current architecture as an asset | ⚖️ | Nothing rewritten. The action bus, ParameterRegistry and waveform renderer are untouched |
| 3 | Product thesis: an adaptive performance environment | 🟡 | **§3 is a list of eleven, and the row used to describe §18.** *The interface picks its density from the window* is a true sentence about a different section, and while it stood nothing anywhere was checking §3's actual content: the eleven things a surface must be able to be — docked, resized, collapsed, expanded, stacked, detached, temporarily surfaced, pinned, contextually promoted, contextually demoted, automatically rearranged. `cockpit::Shaping` is that list in §3's order and words, each with what does it or why nothing does. **Writing it down found three that did not work.** `Placement` carried `size`, `collapsed` and `pinned`; Rust stored them, serialised them and resolved them; and the interface read none of the three — the fifth table in this codebase found parsed and consulted by nothing. A DJ could collapse nothing, resize nothing and pin nothing, and the workspace file faithfully recorded all three. All three are real now: a surface folds to its own header and gives the room back, is resized by dragging the edge that faces the way its dock stacks, and a pinned one survives an arrangement — which is the per-surface half of §78's freeze, and the loudest adaptation there is, since an arrangement replaces every placement at once. **Ten of the eleven happen. The eleventh is a decision, not a gap**: *contextually demoted* is refused because §78 forbids surprise changes and §17 is built to only ever add, so nothing a DJ opened is taken away by a phase turning over — and a row saying "not built yet" would invite somebody to build the thing two other sections forbid. What is left for this row: §3's CRITICAL RULE — presentation priority may change, semantic control identity may not — is honoured throughout and is asserted section by section (§29's one parameter behind every gesture, ADR-0003's action bus) rather than as one statement of its own |
| 4 | Do not build "an AI dashboard" | ⚖️ | Nothing AI-shaped has been added to the chrome |
| 5 | The new GUI model | ✅ | **Both regions exist.** §5A's Mission Bar ships: ten of its eleven readings across the top of the window — phase, occasion, posture, room, tempo, set duration, recording, master bus, device, machine — gathered by `dj_app::mission` in one pass and drawn by `MissionBar.svelte`, which decides nothing. *Compact alerts* is the eleventh and is deliberately not an item: an alert is what a reading at `Level::Alarm` already is, and a twelfth entry restating the others' warnings is the two-descriptions trap. The design rule is §5's own — it behaves like a HUD, so **the normal state of every reading is quiet** and a test fails if a healthy night lights anything up. It replaced a strip that had grown a reading at a time, where the load went amber because one line said so and a recording that had stopped writing was invisible. §5B's performance zone is rebuilt and §7's twenty-three arrangements are the *configurations of one surface system* it asks for. **An arrangement now changes the deck itself.** §5B asks for exactly that — *club mode: large central stacked waveforms, compact decks* — and a preset used to change which panels were open, the deck count, the density and the theme, leaving the deck at whatever the DJ last chose. `Workspace::layout` names a composition the way `theme` names a palette: a **name**, so a DJ who has edited a layout keeps their edit, and empty by default, because most of §7's arrangements are about panels and rebuilding the deck under somebody who only asked for the browser is the surprise §78 forbids. Four of the twenty-three name one, each grounded in §5B's own examples and the layout's own description: *Beginner* and *Club* take `Starter` (the tallest waveform, the least paperwork), *Pro Performance* takes `Pro`, *Laptop Compact* takes `Performance`. **The arrangement keeps what the arrangement states.** A layout carries its own deck count and density, and applying one overwrote both — *Laptop Compact* says Ultra Dense and the `Performance` composition says 0.85, so naming a composition silently moved a band the arrangement had stated. A composition is *what a deck is made of*; how many there are and how tightly they are packed stay the arrangement's. Found by an existing browser test failing, which is the right way to find it. **And §5B's other two compositions now ship.** *Scratch mode: jog surfaces and turntable-oriented controls expand* and *stem performance: large stem-aware waveform and stem controls become first-class* were the half `Layout` could not express — it had booleans for the pads, loops, effects and beat jump and a waveform height, and no knob for a jog size or a stem prominence. It has both now: `jog` in pixels, clamped to the waveform lane's own 48..=320, and `stems_open`. Each travels as a prop on a placement — `deck.jog`'s `size` and `deck.stems`'s `open` — so a DJ's own layout file can ask for either without the renderer learning a new word, and `Scratch` and `Stem Performance` ship as compositions the two arrangements of the same names now take. Each is about **one** control and a test says so against the default rather than against the other, because "bigger than the other one" is satisfied by shrinking the other one. The default is 70 px because that is what `--jog-size: 5rem` has always drawn at this interface's 14 px rem base; setting it to a rounder 80 grew every deck by ten pixels and the density test caught it. **And §5B's supervisory mode is the last of the nine.** *Autopilot supervisory mode: performance display becomes simplified and emphasizes current, next, transition, room response, automation state, emergency takeover* — six nouns, and an arrangement is the only place they can all be true at once. The `Autopilot` arrangement used to dock the booth and the assistant and leave the deck alone; it now names `Starter` — which is *current*, simplified: no pads, no loops, no rack, no beat jump, and the tallest lane of the compositions that ship — and opens the next, room and assistant panels. Which panel carries which of the six is a judgement and it is written in the test rather than left implicit in three surface names: the assistant carries three of them, because `Conduct` inside it is where the takeover press, the authority and the step about to be taken all live. The booth went, on its own `about`: *the things set up once a night rather than reached for during a mix* is the opposite of what a supervisor has open. Driven under Xvfb, one screen carries all six. One thing is worth saying plainly: djmanzo declares a `transition` **surface** and no panel draws it, so *transition* is carried by the assistant — a real mapping rather than a stand-in, but a surface declared and undrawn is a gap of its own |
| 6 | Dock / surface manager | ✅ | Side and bottom docks, several surfaces at once, framed and closable, persisted. `cockpit::Surface`/`Dock`/`Workspace` |
| 7 | Workspace presets | ✅ | 23 of the 24 ship, chosen from a picker in the tool row; each sets its panels, decks, density and theme, and stays editable afterwards. VJ / Visual Performance is absent — there is no visual surface to arrange. **And an edited one can now be kept under a name of the DJ's own**, which is the last step of *starting points, not rigid identities*: before, the edit survived a restart but had no name, so the second layout a DJ built replaced the first and they had a wedding layout and a memory of a club one. `cockpit::keep` refuses an empty name and one djmanzo ships — two rows reading "Club" is a DJ choosing one and finding out afterwards which — and replaces one of their own rather than doubling it, because that is what saving means. The picker lists **Yours** above **djmanzo's**: a DJ who saved a layout is looking for that one, and reading past twenty-three they have never opened to reach it is what saving it was supposed to avoid. `cockpit::workspaces()`, `workspace.spec.ts`, `keep.spec.ts` |
| 8 | Adaptation levels | ✅ | **Level 1 is complete.** §8 asks the software to remember nine things — workspace, density, library columns, sorting, panel positions, preferred decks, favourite pad pages, preferred waveform display, preferred controls — and the only honest way to know how many of them djmanzo kept was to write them down and make a test read the answer. `remembered::Remembered` is that table, and `kept_in` is not a comment: a test reads `state.rs` and fails when a row claims a file nothing writes, which is the failure this codebase keeps meeting from the other side — three fields have now been found stored and consulted by nobody, and a serialised field round-trips perfectly whether or not anything reads it. Five were already kept. **Three were not, and all three failed the same way**: a plain initialiser in a Svelte component, reset on every *mount* — which is every time a panel closes and reopens, not only every launch. *Sorting* went to `sort.json`, so a DJ who put the table in BPM order to find the slow records keeps it when they put the browser away. *Favourite pad pages* went to `pad-pages.json`, and the word is **favourite** rather than last-used on purpose: the pad zone deliberately does not restore the page it was left on, because a DJ who left it on roll an hour ago does not want to come back to a deck whose cues are hidden — a favourite is a decision made once and met on every deck, and starred pages move to the front of the row rather than hiding the rest. *Preferred controls* went to `controls.json` and needed §74's rail to have something it could name: a control existed only as the label it happened to be built with, so `bass in` and `bass out` were two different things. `at_hand::Reach` is the fifteen the rail can hold, each [`Doing`] names five of them, and a kept control is on the rail in every state and marked as the DJ's — keep eight and the rail stops moving altogether, which is a thing a DJ is allowed to want. **The ninth was absent and said so on screen**, because §25's layers were not something a DJ could choose from; they are now, so all nine are kept. The row that admitted it is what made the gap visible in the first place, and the mechanism that let it — a `why_not` sentence held against the `kept_in` column by a test — is still there for the next row added ahead of what backs it. **And the axis itself now exists, which is the whole of Levels 2 to 6.** Most of what those levels *describe* had shipped for a long time under the sections that own each piece — Level 2 is §10's `Posture::Suggest`, Level 3 its `Prepare` and §44's staging, Level 4 its `Assist`, Level 5 is §78's freedoms and §79's locks, Level 6 the autopilot. What did not exist is **the level**: a DJ who wanted *stage things for me but do not move my screen* had to find the posture in one panel and six locks in another, know which of the six mattered, and get both right. `level::Level` is §8's seven as one control, and each row says which posture it sets, whether preferences survive a restart and which of §78's four freedoms the interface keeps. Two pairs share a posture and that is the honest answer rather than a gap: *Static* and *Remember* differ in what survives a restart, and *Assist* and *Adaptive* differ in what the interface may do to **itself**, which is §78's axis and not §10's — a test fails if any step becomes indistinguishable from its neighbour, because an axis with a dead step has fewer notches than it claims. **It sets; it does not own.** One press writes the posture and all six locks, and then leaves them alone — the same contract §7's arrangements and §54's setups state — and `standing` says what has since drifted, so *Prepare, except the theme still adapts* is a sentence djmanzo can say rather than one a DJ has to work out. An axis that owned six switches would be the axis arguing with the switches, and §79's panel would be a row of controls that silently sprang back. **Never chosen is not Level 0**: djmanzo ships suggesting, fitting the density to the window and adapting the theme, so a fresh install reporting Static would be describing itself wrongly and every departure it then listed would be a drift from a decision nobody made. **§9 is enforced here rather than assumed**: a level is autonomy and nothing else, and a test asserts that every question a level answers takes no reading — the tempting version of this table is one where a confident djmanzo quietly acts a level higher, which is §9's *low confidence + high autonomy = invalid/unsafe* arriving through the back door |
| 9 | Separate autonomy from confidence | ✅ | `dj_assistant::Warrant`. The invalid cell is **unrepresentable**: `Act` and `Mix` carry a `Grounds` whose constructor is private and refuses a certainty below `Fair`, so no caller anywhere can write down a warrant to act on a read something disagrees with. Asserted over the whole 6×3 matrix, and the consequence — an unclear night stages instead of mixing — is asserted in the autopilot |
| 10 | AI posture stays compatible with djmanzo's | ⚖️ | The six postures and nine occasions are untouched |
| 11 | Add a context engine | 🟡 | `dj_core::ContextEngine` ships and is the common input: the phase, its certainty, what produced it and which way the evidence disagrees, from two sources and nothing else — what the DJ declared, and where the last few minutes sit in the whole night's own range. `dj_app::night` feeds it from the snapshot the pump has just built. **And all eight of `DJContext`'s fields are now one object, gathered in one pass.** Five were already real and that is exactly what was wrong with them: they were published by five different things on five different schedules, so "the context engine" was a phrase rather than an object and a consumer wanting three of them asked three questions and got three answers about three different instants — §11's own *do not duplicate context logic inside each component*, with a clock in it. The three that had no gatherer at all have one now. **`musicContext` is about the records, not the bus**: the tempo comes from a deck that is *playing*, and with two playing the louder fader wins, because the fader is the DJ saying which record the room is hearing — taking `decks[0]` would make a record cued at 174 on deck 3 the tempo of a 124 BPM set. **`hardwareContext` tells "no controller" apart from "no MIDI service"**, which is the difference between a problem plugging something in fixes and one it does not. **`djBehaviorContext` is counts and rates, never a judgement** — §13 at the scale of one night: *forty gestures a minute* is a fact and *this DJ is frantic* is a claim nothing has earned, and the commonest gesture stays absent until §13's own threshold clears it. `context::FIELDS` holds §11's eight names beside djmanzo's own and a test reads the serialised object against it **in both directions**, so a field quietly dropped and a field quietly added fail alike. Drawn in the Night panel, folded, because it is evidence for the sentences above rather than something to read every night — a panel leading with eight rows of machine state is the AI dashboard §4 forbids. Every unmeasured field reads *not read* rather than a neutral value: a room with no camera has not been read, and a zero there would be a measurement nobody took. **And §11's nine consumers are a table now too, held against the code.** The row used to say two of the nine read the engine; that was a sentence in a status document with nothing checking it, which is the same failure one layer up from the one `FIELDS` catches. `context::CONSUMERS` names §11's nine, the module where each reading happens and the function or type it arrives through, and a test **reads that module's source** and fails when a row claims a reader that has stopped reading — the `Remembered::kept_in` pattern, pointed at consumers instead of files. Writing it down put the count at **eight of nine**: the adaptive GUI, the theme engine, suggestions, the assistant, audience sensing, visualization (through the theme, which is djmanzo's visual language) and automation policy all read it, and §17's work an hour earlier is why suggestions is on that list. **The ninth was technique recommendations, and it is closed here.** The coach narrowed its curriculum by §16's pack and by what the DJ had done in the last two minutes, and had no idea whether they were mid-blend or standing between records — so a new move to learn could appear in the middle of a mix. §58 names technique advice in the **contextual** tier in as many words and §18's mixing budget leaves room for the first two tiers and no others, so this is not a new rule: it is two tables that already existed being asked the question they were written to answer, at the one place that had never asked it. **The correction is deliberately not gated** — "both records have their bass up" is the one thing a coach exists to say during a mix, because it sounds fine in headphones and wrong in the room — and **withheld says so** rather than going blank, because a learner shown nothing the moment they start a blend would read it as having finished the curriculum, every single mix. The line does not make the panel appear, which is the §18 half: a section opening mid-mix to say "not now" would be the interface moving while somebody is reaching for it. What is left for this row: **session planning**, and that is a refusal rather than a gap — a set arc is a statement about a stretch of time and a phase is a reading of one minute, so defaulting a four-hour journey from whatever the music was doing when the button was pressed would be djmanzo deciding the shape of the night from its first record |
| 12 | Learn the DJ | 🟡 | Taste learned from play history ships, and **persona learning now has its shape**: §13's `Tendency` for gestures and §81's `Profile` for whole kinds of night, both constructor-enforced so neither can generalise on too little. **And the other half now ships: the rail reads tonight's profile.** Once the DJ has named the night and there are three of that kind behind it, a record's genre moves its score by how much of that kind of night it usually is — added never multiplied, log-scaled so twice as often and half as often are equal and opposite, and bounded at three quarters of a point. That bound is the design: a same-key match scores three and a key clash minus two and a half, so a profile can reorder records that would **all** work and can never lift one that would not. A profile breaks ties; it does not overrule the mixing. The rail says it is conditioned and names the evidence, in Rust's own words, because a ranking a DJ cannot see the reason for is one they cannot argue with. **And the other two now act.** §12's list ends with *decay* and *explicit user preference*, and the middle of it — recency, frequency, context, confidence — is what a profile already is; what was missing was anything doing something with it. `profile::fits` turns tonight's profile into what djmanzo may change about itself, and §81's row holds the rules. **And decay ships, which completes §12's own list.** The claim that §14's gestures had no older evidence to fade was the fifth stale reason this project has taken back: they are persisted per night in the `nights` table — `Profile::techniques` has read them for as long as it has existed — and so are the density, the transition style and the posture. What was actually true is that `profile::profiles` counted every night equally, so an evening two years ago voted exactly as loudly as last Saturday: a DJ who spent a season fading and has cut every night since was still told "you mostly fade", and the planner was still handed Fade, for as long as the old season was one night the more numerous. Each night now weighs `learned::recency(began_at, now)` — **the same curve and the same half-life taste already uses**, made public rather than copied, because two half-lives that drifted apart would be djmanzo disagreeing with itself about how long ago last spring was. `agreed` and `usual` weigh rather than count; the **share** is still a share, so `AGREE` means what it always did and a profile whose nights are all equally old behaves exactly as it did before decay existed. **It never reaches nought**, which is the half that keeps it honest: a DJ who has not worked since last year is told what they used to do rather than told nothing, because djmanzo has no *newer* evidence, which is different from having contrary evidence. Decay changes an answer only where old and new disagree, which is the case §12 asks for. **The count of nights is not faded** — how much evidence there is is a fact, and fading it would make djmanzo understate its own history. Driven under Xvfb against six wedding nights two years old that faded and three from last week that cut: the assistant reads "over 10 nights: mostly cut transitions", and every rail row whose keys agree reads `32-beat cut` while the two whose keys clash stay at `32-beat echo` — §12's decay, §81's style and the planner's bound composing on one screen. What is still missing from §12's own list: nothing |
| 13 | Never learn badly | ✅ | A **type**, not a warning. `dj_app::signals::Signal` carries the gesture and the phase the night was in as one value that cannot be taken apart, and `Tendency` — the only thing that generalises — is constructible solely through `tendencies()`: never without a phase, never on fewer than four occurrences *in that same phase*. So the directive's own bad sentence, "user likes enormous BPM jumps", is not one this workspace can produce; "you sometimes move the tempo by hand when the night is at its peak" is. The wording is written in Rust, so the interface cannot make a claim Rust would not |
| 14 | Behavioural signals | 🟡 | Twelve of §14's twenty gestures, derived from the action log rather than recorded separately — so a set from before this existed has them too. Coarser than the vocabulary on purpose: six EQ verbs are one gesture a DJ would name, and counting per verb would need six times the evidence. A test asserts every gesture named is one the bus can produce. The other six — track searched, previewed, staged, candidate rejected, candidate selected, assistant suggestion accepted — are not actions and are absent rather than approximated. **Decay ships now, one layer up.** This row said decay waited on sessions being read back across nights, and that was true of the *log* and misleading about the evidence: the gestures that generalised are written to the `nights` table at the end of each night, so §81's profile has had cross-night gesture evidence all along and was counting every night equally. `profile::usual` now weighs each night on `learned`'s hundred-and-eighty-day curve, so a habit given up fades out of the profile instead of outvoting the present forever. Within a single night the window is still tonight's log and that is right rather than pending: a gesture decaying over the four hours it was made in would be djmanzo forgetting the first hour of a set it is still playing. **And *previewed* is one of them now**, which makes thirteen. Nothing in `signals` was rewritten to do it: §22's audition shipped, `SessionEvent::Auditioned` is the log seeing it, and `did` picked it up — the gesture became one the log could record. An audition earns a place in the log in a way *track searched* does not: a search is a query with no object, and an audition is a **decision about one record**, with a time on it, which is the shape §14 counts. It names no deck, because there is no deck — a field carrying one would be a number every reader has to know to ignore. **A replay does not perform it**, and that is a decision rather than an omission: a replay re-performs a set, and an audition was the DJ listening privately to a record they very often did *not* play. Re-performing it would put a record nobody chose into the headphones of whoever asked to hear the set. Asserted as byte-identical audio, because the failure to guard against is the audition being delivered as *something*. **Adding it found the coverage test not covering.** `every_gesture_named_is_one_something_can_produce` exists to fail when a gesture is named and nothing produces it — its own doc says that is worse than not listing it — and it wrote the twelve out again instead of walking `Did::ALL`, so a variant added without a producer passed. A new variant was added and nothing failed, which is how it was noticed. It walks the list now. What is left for this row: the five of §14's that are not actions — track searched, staged, candidate rejected, candidate selected, assistant suggestion accepted. They are not waiting on machinery the way *previewed* was: a search is a query with no object, and the other four are verdicts a DJ gives a row in a list, kept out on §13's grounds rather than engineering ones — a rail's pass is about this minute, and writing it down as evidence is how "not that one, now" becomes "never suggest this again" |
| 15 | AI should understand DJ technique | ✅ | The technique catalogue ships |
| 16 | Domain knowledge packs | 🟡 | **Packs ship as a format.** `dj_assistant::pack::Pack` is eight bodies of DJ knowledge, and each one *selects* rather than restates: it names the `dj_core::genre` families this kind of DJing turns on, the `technique::Kind`s it teaches, the hardest move it will send a DJ at, and the §81 occasion whose presentation it pairs with. §16 lists fifteen fields a pack can define and djmanzo already holds most of them in the places that own them — the technique catalogue, the genre map, `shape`, and §54's `setup` for the whole presentation half — so a pack that restated any of them would be a second copy of a live table, and the copy is the one that goes stale. **It is read, not merely stored.** The consumer is `coach::next_lesson`, which used to pick the easiest unshown move in the whole catalogue: a bachata DJ was eventually sent to learn a transformer scratch, and a turntablist was taught the bass swap they had been doing for years. Teaching *inside* a pack is the difference between a catalogue and a curriculum, and it is the one assertion that can tell a pack that works from one that is merely written down. **The picker shows what choosing one costs** — how many of the catalogue's moves the pack would teach — because a pack is a narrowing and a DJ pressing one deserves to see how far it narrows. §16's own last line is *do not hard-code this logic into UI components*, and a Rust test reads every Svelte file and fails on a genre family written out as a string literal. **Eight, not thirty.** The eight are the ones whose content djmanzo can justify from tables it already has; a *Karaoke / MC* pack asserting a technique list nobody checked is a curriculum that teaches the wrong thing with confidence. The format is the deliverable; the rest is content, written by somebody who does that kind of night. **And the pack now reaches the ranking, which is where a narrowing is actually worth something.** `families` had been parsed, stored and published to the interface and read by no part of djmanzo — the sixth table found in this state. `commands::with_pack` folds the chosen pack into the Next rail beside §24's kept pairs and §12's profile: a record in a family the pack turns on is credited `Reason::InPack`, and where the pack says this music does not pair across half and double time the credit the scorer gave for exactly that is **taken back and said** as `Reason::HalfTimeUnusual` rather than a penalty being added, so the pairing scores what an unrelated tempo scores — which is what "not part of this music" means, where a penalty would rank it *below* a record with nothing in common. That covers §16's *genre relationships*, *suggestion weighting* and *half/double-time relationships*. **Bounded, and now bounded as a group.** Three quarters of a point, the same as taste and §81's profile and quoted from the same scale a same-key match is worth three on; each of the three was bounded in prose in its own file and nothing checked what they do when they all point the same way at once, which is the ordinary case — a Latin DJ at a Latin night with the Latin pack chosen gets all three. A test now holds their sum below the gap between a key match and a key clash. **Empty `families` is open format and a real answer**: no record is credited, because a DJ who plays everything has no family to centre on. *Transition preferences* is deliberately still unowned rather than threaded through `plan::choose_style`, whose refusal to blend records that cannot be blended must not be overridable by a pack. And the rail re-asks when the pack changes: it does not poll, so without that the DJ presses *Latin* and keeps reading a ranking made for another night until a deck happens to move. What is left for this row: the remaining twenty-two packs, and the §16 fields no table yet owns (energy curves, allowed BPM relationships, transition preferences) |
| 17 | GUI adapts to session phase | 🟡 | **Phase-driven layout ships.** `cockpit::priorities` is §17's table — the six phases it names mapped onto djmanzo's five plus the stretch before anything has read, which is §17's *Setup* — and the shell opens what the phase asks for when the phase turns over. Three rules make it bearable rather than hostile. It is gated on **§18's `reflow`**, false during every mix, so a phase turning over mid-mix is *skipped* rather than queued: moving a panel while somebody is reaching for it is the failure adaptive interfaces are hated for. It only ever **adds**, so nothing the DJ opened is taken away and closing one of its panels is the override §17 asks for, working without a dialog. And **peak promotes the least** — that phase's list ends with *minimal UI clutter*, which is an instruction about the five words before it, and a test keeps it the shortest. **And the half that is not surfaces now exists too.** *Gradual energy*, *energy trajectory*, *harmonic resolution*, *crowd cooling* and *known anchors* are instructions about what djmanzo should **offer**, and this row used to say they "belong to the planner" — they belonged to nobody, and the rail opened on *Hold* at every hour of every night whatever the night was doing. `asks::asks` is one row per phase: which way the rail ranks, the one thing the phase asks it to prefer beyond that, and §17's own words for the line the rail draws. The rail starts from it and `commands::with_phase` folds the preference in beside §16's pack and §24's keeps. **Two phases ask for something and four ask for nothing**, which is a real answer rather than a thin table: most of a night the direction is the whole of what a phase has to say, and a fold that credited something anyway would put a chip on every row saying nothing. Release asks for *a key that settles* — read off the scorer's own key reasons rather than recomputed, so the rail cannot disagree with the chip beside it — and closing asks for *a record you play*, played at all rather than played often, because a threshold would be djmanzo deciding how many times counts as known. **It is a starting point, not a setting.** §17's last line is that the DJ must always be able to override the inferred phase, so one press of the direction takes the rail over for the rest of the night and it never goes back to following on its own; the line saying what it is following disappears with the same press. The comment on that control used to say the direction was *the one thing the ranking cannot infer*, which had quietly stopped being true. **Adding it made the tilt bound a four-way problem** and the test that holds all four below what a single key relation is worth is where a fifth will fail. What is left: *longer transitions* and *longer blends* are already true — the planner proposes the longest transition that fits, every time, and a knob saying "prefer longer" would be a second description of that. *Low suggestion noise* is §18's budget, which is **widest** in warm-up, the opposite of what §17's phrase asks for: §18 sizes by what the DJ is doing and §17 by what the night is doing, and reconciling them belongs to whoever owns the budget rather than to a second budget here. *Stems* and *FX* are drawn on the deck and have nowhere to be promoted to |
| 18 | Attention budget | ✅ | `Attention::for_context` derives it in one place from the context engine, in order of severity — something broken, two records audible, peak time, room to think — and it is published on the snapshot and written onto the root as `data-motion`, where the stylesheet honours it. The rule that matters is enforced rather than hoped for: while two records are audible the interface may not reflow. **And the budget's other sentence is data now.** `Attention::performing` had said *"Tier 1 and 2 only; nothing else may take room"* in a doc comment since it was written, where nothing could read it; `room_for` carries it as §58's tier, all four budgets name one, and `quieter_of` takes the shallower of two — so §77's chosen focus narrows it like everything else. What spends it is the command palette: mid-mix the twelve rows become the four a hand needs, and the palette **says so**, because a list that halved itself in silence is a list a DJ stops opening during the minutes it exists for. The escape hatch is the point of the sentence — the budget governs what djmanzo *offers* and never what a DJ asks for by name, so `settings` typed mid-mix still finds Settings and claims nothing about having been shortened |
| 19 | No random UI reorganisation | ⚖️ | Enforced by a golden-order test: the deck's control order is asserted in full and fails if anything moves |
| 20 | Playlist / library overhaul | ✅ | Function tags and their filtering ship, and **all four views** do. **Set Flow**: a dockable surface drawing the plan as a sequence with the seam between each pair — its deltas, its confidence, and whether it needs a cut rather than a blend. **Pair**: its own surface, the two records side by side with the seam between them, each with its waveform, and the mix point drawn *on* the outgoing one. **Cards**: sleeves read out of the files' own tags and served on their own URI scheme, each card carrying its operations — deck, set aside, more like this, favourite, and the reasons when the list is a suggestion; *preview* and *compare* are named as absent rather than faked, and *stage*/*prepare*/*queue* are djmanzo's one set-aside gesture. **Performance table**: fifteen of §20's twenty columns, and the *instant custom column configuration* the section asks for — a picker beside the view switch, not in Settings, because "instant" is the instruction. The gap was never the data: a library row already carried the year, the loudness, the phrase length, the rating, the play count and when it was last played, and every one of them was read off the disk and thrown away. `columns::Column` is the list, with each one's heading and its sentence, so the word in the picker is the word at the top of the column; one `{#each}` drives the header and the cells, because those are two descriptions of one thing and every other place that was true here has a scar; and a Rust test reads `Library.svelte` and fails when a column Rust offers is one the table cannot draw — §7's presets fell into exactly that trap. Three rules: an unknown slug is dropped rather than refused, so a preferences file written by a later build opens in an earlier one; a column named twice is drawn once; and **the title is not a preference** — a table without it is a list of BPMs with no records attached, so Rust puts it back and the picker disables the box rather than letting it silently re-tick. Readings are drawn as readings: a rating is stars because "4" looks like a measurement, and *last played* is an age because the question that column is asked is *was this in last weekend's set*. **Five of §20's twenty are absent, each for a reason rather than an oversight.** *Camelot* is not a second column — djmanzo's key **is** Camelot. *Colour* is drawn as a stripe down the title, because a DJ colours records to find them at a glance and a column of swatches is a column to read. **Fifteen of twenty now, and one of those two reasons had gone stale.** *Vocal availability* is answered: §25's `presence` measurement asks exactly the question this row said djmanzo never had — and the column stores the **strongest window's share** rather than a verdict, because the line between *has a vocal* and *does not* is `presence::STRONG`, the one stated guess in that measurement, and baking today's guess into a DJ's database would mean re-analysing a library to re-tune it. Deciding on the way out means re-tuning re-reads the column. Three answers, kept apart end to end and mutation-tested through SQLite: a mark for a record with a lead in it, a dash for one measured and found to have none, and a **blank** for one nobody has analysed — a DJ hunting for an acapella acts on *there is none* and skips *no answer yet*. **Adding it found the fixture had been drifting.** §20's column list was typed out by hand in the browser harness under a comment claiming fourteen while holding fifteen, and nothing compared it with `Column::ALL`; the sixteenth column simply had no row to tick. It is a golden file blessed from Rust now, like §54's setups and §16's packs. *Stem availability* is the one still open and the reason has changed rather than held: the separator can split any record on demand, so *availability* here means **ready**, not possible — and the stem cache is keyed by chunk, so there is no cheap per-row answer to give a table that draws a hundred rows. *Transition suitability*, *AI confidence* and *function tag* are §76's lens, which is already a column set with its own switch, and offering them twice would give one judgement two homes. *Request count* belongs to a night rather than to a record. **And §20's energy is measured now, rather than loudness wearing its name.** The library had a Loud column and no energy one, because integrated LUFS is what djmanzo could measure and `dj_library::suggest` said so in its own words: *a real energy measure — spectral flux over the track, percussive density, dynamic range — belongs in `dj-analysis` and is not here yet*. `dj_analysis::energy` is those three. **Flux** is how much the spectrum moves, averaged over the record. **Drive** is percussive density in the low band, counted **per beat** and by local peaks rather than by hops over a line — two earlier versions counted hops and then rising edges, and both made the same pattern read as less driving at 174 BPM than at 120, which is tempo wearing energy's name in the other direction. **Punch** is dynamic range inverted, because a record whose loud moments sit close to its quiet ones is relentless. The three are carried beside the total, on §42's posture: a number whose reason cannot be seen is one nobody can argue with. Both columns ship, because they **disagree** — that is the whole reason for the second one, and the browser test that matters asserts the two rank the fixture's records differently. A track analysed before the column existed reads blank rather than zero, which is the library's standing rule about a measurement nobody has taken. What is left for this row: nothing this container can build — *vocal* and *stem availability* need analysis nobody has written, *transition suitability*, *AI confidence* and *function tag* are §76's lens and would be one judgement with two homes, and *request count* belongs to a night rather than to a record |
| 21 | "Prepare" must be first class | ✅ | Its own dockable surface beside the browser, not a strip inside it. One gesture — `→` on a browser row — hands a track over; `prepare.svelte.ts` is the only path between them, so there is no second, differently-behaved way to set a track aside |
| 22 | Next-track rail | ✅ | The rail ships as its own dockable surface, following whichever deck is playing: up to eight candidates, each with one line of deltas (`+3 BPM · 8A→9A · +1 dB`), a confidence bar, and load / set aside / more-like-this / pin / pass. The **estimated transition type** ships beside each candidate — `32-beat blend at 2:09`, on its own line under the deltas, because one is about the two records and the other is about the mix between them. It is §27's `ghost::look`, so the rail, the ghost overlay and the mix djmanzo performs are one plan seen three times, and the phrase itself is written once in Rust rather than spelled twice. An unanalysed record gets no line rather than one borrowed from the outgoing tempo. **And the fifteenth ships: audition.** The reason filed against it — *needs a preview player djmanzo does not have* — was wrong, and finding out how wrong was the work. A sampler slot already takes the same `Arc<dyn TrackSource>` a deck takes, already plays it, and already routes to the headphones alone: `SampleOutput::Cue` exists and its own doc says why. One thing was actually missing, and it is the thing that separates an audition from a sampler slot: a slot always starts at zero, and a preview that opens every record at 0:00 plays a DJ the least informative sixteen bars of every candidate in the list. `dj_engine::preview` is a voice of its own — not a reserved sampler slot, which would take a pad away and stop whatever it was holding — and it **cannot reach the room structurally**: there is no `output` field and no branch that sums it into the main bus, so the routing mistake that ends a set cannot be made. A machine with no cue pair therefore auditions in silence rather than out loud, and the playhead still runs so the interface is not left drawing a frozen position. At the record's own speed, and the machinery to stretch it to the master tempo is right there and deliberately unused: the rail already answers `+3 BPM` on every row, and pitching a vocal up two semitones would lie about the one thing a DJ is listening for. **Where it starts is a decision, and it is said out loud**: `audition::start_of` takes the first drop, else where the vocal enters, else the top — and the rail prints *from the drop* with the position, because a preview that opens an unfamiliar record ninety seconds in without saying so is indistinguishable from one that opened the wrong record. The top is not a guess dressed as a choice: a record nobody has analysed has no landmark, and inventing one would be a number with no reading behind it drawn as confidently as the two above. **It does not load, stage or write anything down** — §22 lists audition and load as two of six, and a preview that counted as a play would put every record a DJ listened to into their history and into §12's taste — and a browser test asserts exactly that, beside the ghost's own. It is a line rather than an `Action` for ADR-0003's reason, the one a load is: it carries a decoded record, and it takes an id rather than a path so nothing reaching the bus can make djmanzo read an arbitrary file. What is left for this row: **nothing djmanzo can do here**. Zero allocations on the audio thread is asserted the way the sampler's is, over sixty-four auditions while a record plays — but this container has no sound card, so nothing has heard an audition and nothing claims to |
| 23 | Track function tagging | ✅ | Ten functions, closed vocabulary, migration 10, browser picker, and `for is opener` in smart folders |
| 24 | Pairs and relationships | 🟡 | **"Save this transition"** ships: a *keep* on any of tonight's mixes stores the pair, directionally, with what the mix was — and keeping the same one again strengthens it rather than duplicating it, which is §24's confidence weight as a count. Only what was *kept* is stored: every mix a night contained is already derivable from the log, and a second copy of those would eventually disagree with it. The Next rail answers "why do I keep seeing these two together?" with a reason placed first, weighted below a key match and a tempo match together — a strong reason, not an override. **And a kept pair now carries the loop it was held on**, which is §24's own example of a learned relationship: *"A into C works only with an 8-beat loop"*. The qualification is the content — a pair kept without it records that a mix happened, a pair kept with it records how — and it is read off the action log rather than asked for, so a set recorded long before the column existed still answers it. `mixes::Handover::loop_beats` tracks every loop gesture the log holds (set, by phrases, halved, doubled, released) and takes the one running on the record **going out** at the moment the mix began, because that is the record the technique belongs to. It scores nothing: two DJs who each kept a pair once have said the same thing about it, and ranking the one who happened to be looping above the one who was not would invent a preference neither expressed. What it changes is what the rail can say — *kept ×2 · 8-beat loop* on the summary line, *you kept this mix 2 times, on an 8-beat loop* on hover — and that is the half a DJ can act on when the record is eight bars from the end. Driven under Xvfb with a real library migrated 14 → 15 in place, its existing kept row intact and its new column null, which is what "kept before this existed" should read as. What §24 also lists and this lacks: "A vocal → B instrumental" is now derivable — the style is in the log and `dj_app::shape` says what each style does to the stems, so a kept vocal drop *is* a record of one — and the one thing left is "often selected after a crowd-energy drop", which needs crowd sensing this container has no camera or microphone for |
| 25 | Waveform overhaul | 🟡 | The architecture ships: `dj_render::layer` names §25's twenty layers, what each encodes, which half of the renderer draws it and whether it exists — and, after them, the layers other sections asked for. §25 says *potential* layers, and that word is doing work: its twenty are a floor rather than a ceiling, and `transients` is there because §75 asks for transient density and §25 never named a layer for it. The test that guards the list holds §25's twenty **by slug** and requires anything beyond them to declare which section wanted it, which is strictly stronger than the count it replaced: `len() == 20` catches a layer being dropped only if nothing was added in the same breath, and it made adding one look like breaking the directive. **Fifteen do** — amplitude, spectral balance, beats, phrases, downbeats, cues, the loop region, the seam, the mix-out window, §27's ghost, the grid's own uncertainty and the runway. A golden file and a browser test keep the count honest in both directions: everything on screen carries a `data-layer` that must be in the table, in the overview as well as the lane. The eight that do not exist are named rather than forgotten, and reserve no colour. `suggestion` shares the `proposed` colour with `mix-out` deliberately: both are djmanzo saying *could*, which is the distinction §57 asks for keeping, not the one it warns about. **The inventory is now a picker**, which is what closed §8's ninth row: §8 Level 1 asks djmanzo to remember a *preferred waveform display*, and §25's table is what that display is made of. Ten of the twelve can be turned off. The two that cannot are amplitude and spectral balance — those *are* the waveform, and a picker offering to remove them offers an empty strip — so the box is shown, disabled, and says why, exactly as §20's title column does; the eight nobody has built are offered the same way rather than left off the list, because *twelve of twenty* is a fact a DJ is entitled to see. Both halves of the renderer honour it and they honour it differently: the seven overlays are elements the interface stops drawing, and the three grid layers are rasterised in Rust and travel in the **tile URL**, where they join the theme as part of the cache key — tiles are `immutable` for a year, so a DJ turning the beat lines off would otherwise keep being served the tiles already drawn with them. A Rust test reads both strips and fails when a layer the picker offers is one nothing consults, which is the failure mode that would make the whole picker read as decorative. **And three of the eight are built now.** `breakdowns`, `drops` and `energy` had "the analysis does not exist" beside each since this table existed, and `dj_analysis::energy::trajectory` is that analysis — the same banded onset curve the phrase detector reads, because a record's shape over time and its phrase boundaries are two questions about one measurement. A record is measured in windows of one phrase (eight beats where it has no phrase structure); a **breakdown** is a run of at least two windows whose low band falls to six tenths of the record's own middle, and a **drop** is the window after one where the low band comes back half as much again. All three share one colour — `Role::Shape` — on the same argument `proposed` is shared by two: they are one reading of one curve drawn three ways, and three hues for three parts of one answer would be §30's neon-everything failure rather than §57's distinction. **Sixteen of twenty, and the sixteenth was not waiting on an analysis at all.** `mix-in` — *where a record could be brought in* — was counted among the five that need something djmanzo does not have. What it actually needed was `plan::mix_out` read from the other end plus the first drop `energy::trajectory` has been finding since §75. `plan::mix_in` is that: it **opens** at the record's first whole phrase, snapped *forward* rather than back because there is audio after the grid anchor and none before it, and it **closes** at the record's first drop, snapped back to a phrase — a mix started after the drop has thrown away the thing the transition was building towards. Where nobody has found a drop the close is the longest transition the planner proposes, measured from the opening, and `MixIn::before_a_drop` says which of the two it is — the same honesty `MixOut::on_phrase` carries, because a window that dressed arithmetic as the music would be the confident lie `plan` is written not to tell. A record whose drop is inside its first phrase gets no window at all, because a band drawn backwards is worse than no band. **It shares `Role::Proposed` with `mix-out` and §27's ghost**, and that is the clearest case for sharing there is rather than a stretch: all three are djmanzo saying *could* about the same mix, and two colours for the two ends of one question would say they were different kinds of claim. §57's rule is enforced by a test that lists the sharers, so adding the third was a decision made where the rule lives — and both that guard and the built-count guard fired on the way, which is them working. Drawn on the lane and on the overview for the reason `mix-out` is on both: on the lane a window at the *start* of a record is only visible once the playhead is near it, and the whole point of it is to be read while deciding whether to load the record. **Seventeen of twenty, and the seventeenth needed no analysis either.** `saved-loops` was filed beside the four that wait on something djmanzo cannot compute, and `dj_library::StoredLoop` has held them per track for as long as the library has — a DJ could save eight loops, recall any of them from a pad, and never see where one was. `waveform_info` carries them now, from the **library** rather than from the deck's grid, and that split is deliberate rather than an inconsistency: a mix window is beats counted from a grid, so it has to come from the grid the beat lines are drawn from; a saved loop is two frame positions in a file, so editing the grid moves the lines and leaves the loop where the DJ put it. **The colour question had an answer already.** `Role::Looping` — *a stretch of the record that repeats* — is what an armed loop and a kept one both are, so they share it, and §30's rule is that a *state* is painted with the role it means: the armed band is solid, the kept ones are quiet and carry the slot number, which is the number a DJ presses to fire one. A second hue would have said they were different kinds of thing rather than the same thing not firing. **Ordering is the store's, not a second sort.** `saved_loops_of` sorted the rows itself at first and mutation testing found the sort did nothing: `Library::loops` selects `ORDER BY slot`. The sort came out and the test now holds that contract from the consumer's side, so dropping the clause fails rather than silently putting the wrong number on a band. **Eighteen of twenty, and the eighteenth had been sitting in the repository the whole time.** `vocal` — *where somebody is singing* — carried *the analysis does not exist* beside it for the life of this table, while djmanzo shipped `dj_stems::hpss`: a separator that pulls a vocal stem out of a record with **no model, no download and no runtime**, and which a DJ can already solo. The waveform could not draw where that voice was. `dj_analysis::presence` measures the same thing the separator defines — the harmonic part of the centre channel between 200 Hz and 8 kHz — and measures it **without reconstructing anything**: a presence curve is a question about the masks, not about the audio, so there is no inverse transform and no second copy of the record. The band edges moved to `dj_core` so the separator and the measurement cannot drift apart. **On one point it deliberately asks a sharper question than the separator.** A separator has to produce audio that sums back to the mix, so its centre is the mid channel as it finds it — and a hard-panned guitar appears in mid at half amplitude whether anyone wanted it there or not. The first version of this read that guitar as *half a voice*, which is what the tests caught; a measurement is under no such constraint, so each bin is weighted by how centred it actually is. **Where it is wrong is stated rather than buried**: a centred synth lead reads as a voice, and the layer's own one-line description says so — *where a lead is centred in the voice range, usually a singer*. `Role::Uncertain`, which is what the role's own doc reserved it for years ago: *vocal and stem presence will each arrive with a confidence of their own*. **Which found a real defect in the palette.** `--uncertain` and `--shape` were both `var(--text-dim)`, justified by a comment saying one was a hatch on the lane and the other columns on the overview, so there was no glance in which a DJ had to tell them apart — and this layer is an estimate drawn on the overview beside the trajectory columns, which is exactly that glance. `--uncertain` is now a tint of the palette's own accent, derived so every package gets one free, and a vitest fails the day somebody points the two at one token again. **And driving it found a limitation in the picker rather than in the layer.** §8 stores a DJ's chosen layers as an explicit list, and an empty list means *everything* — so a DJ who ticked all twelve available layers a year ago now sees twelve of eighteen, having never been asked about the six added since. The rig showed it first: the new strip drew nothing until a stale `layers.json` was removed. Named rather than fixed, because distinguishing *I chose everything* from *I chose these twelve* is a change to what §8 remembers rather than to what §25 draws. **Nineteen of twenty, one commit later, and the nineteenth came out of the eighteenth's own pass.** `stems` — *which of the four currents is carrying the record here* — was the last layer filed under an analysis djmanzo could not do, and the measurement written for `vocal` was already computing the masks the other three need. It answers as a **partition**: the four shares of a window add to the whole of it, which is the numerical form of the separator's own central invariant and is what makes them one reading rather than four that could disagree about how loud the record was. A test walks every hop and says so. **It does not share `vocal`'s colour, and the layer table's own prediction that it would was wrong.** `Role::Uncertain`'s doc had said for years that *vocal and stem presence will each arrive with a confidence of their own*; building it showed the difference — vocal presence is one number and can be drawn as strength, stem presence is a question about *which of four*, and a drawing that cannot say which answers nothing. `Role::Stems` draws each window in the colour of the fader that mutes the current it names, which §30 already holds apart in every palette. **The dominant current rather than all four stacked**: four segments in a four-pixel band is one pixel each, which is texture rather than information, and §25's question has one answer per window. The four stem labels, keys and colour tokens moved out of `Stems.svelte` into `ui/src/stems.ts` on the way, because a second copy of that order is how the bass fader ends up coloured like the vocal band. What is left for this row: **the crowd**, which needs a camera or a microphone this container does not have — twenty-one of the twenty-two are drawn now, §75's `transients` and §116's `melody` having joined them — and **the limitation recorded against it is fixed, and the reason given for it was wrong.** The row said a loop saved mid-set would appear only when the record was next loaded, because the waveform re-asks on a load and on an analysis and a stored loop changes neither. That was reasoned rather than observed, and observing it found something worse: the lane and the overview were re-asking **on every frame**. Their `$effect`s read `deck.analysis` and `deck.length_frames`, which look like fine-grained dependencies and are not — `App.svelte` replaces the whole snapshot sixty times a second, so every deck is a new `$state` proxy and every read through it a new signal. Measured: **forty `waveform_info` calls for ten frames of ordinary playback** — and, once the whole seam was measured rather than the one call, `phrase_grid` and `pad_pages` doing the same thing for a total of forty-two, each one a library query and a clone of the whole trajectory — precisely what that call's own doc says it exists to avoid. `ui/src/waveformAsks.ts` builds a key from the primitives the answer depends on and the effect returns early when it has not moved: the same ten frames now cost **nothing**. The third thing to watch is `snapshot::Marks`, a generation per deck bumped where the library is written rather than where the interface asks — so a loop saved from a controller pad reaches the lane on the same terms as one saved from a click, and a grid edit does too, which `grid_confidence` could not carry because the first hand edit takes it to certain and every edit after leaves it there |
| 26 | Direct manipulation on the waveform | 🟡 | **Five of §26's nine, including the two it names first.** The mix point is a handle on the outgoing waveform, **hot cues are handles on the deck's own lane**, and **a loop has a handle at each end** — drag any of them, or use the arrow keys, shift for a coarser nudge. The waveform reports a *position* and nothing else: Rust decides what landing there means, and for a cue that is `Deck::move_hot_cue`, which clamps into the record, snaps to the grid when quantize is on, and **refuses a slot that holds nothing** — a drag begins on a marker that is on screen, so a move addressed at an empty slot did not come from one and creating a cue there would be a mark the DJ never made. `HotCueMove` is its own action rather than `HotCueSet` with a parameter: that one is a DJ *listening* and puts the cue at the playhead, this is a DJ *looking*, and a control that conflated them would move a cue to the playhead every time somebody dragged it. *Suggested transition* is the mix point and *AI suggestion: show as a ghost layer* ships with §27. A loop edge is `LoopEdge(Edge, FramePos)` and `Deck::move_loop_edge`, which is a **resize rather than a slide**: the other end stays where it is, which is the whole difference from `loop_move`, and the load-bearing test pins exactly that. An edge dragged through the other one is clamped to the shortest loop the halve button already obeys rather than swapped — a DJ pulling the out point back past the in point is asking for the shortest loop, not for the loop to turn inside out. **Six of nine now: the phrase marker ships** under §75, which is the section that asks for it as a *control* rather than as a drawing. `grid_phrase` is a verb the parser accepts, so the drag is the same event as typing it or mapping a controller to it; the boundary snaps to the nearest beat, because a phrase begins on a beat by definition and a marker half a beat out is not a phrase structure; and it is the one grid verb that leaves the grid alone — every other edit clears the phrase because it counts from the old anchor, and this one moves the phrase itself. **Seven of nine: the transition end ships**, and the reason standing beside it — *needs an owner that knows what moving it means* — had already stopped being true when it was written. `Transition::set_length` has clamped a length to what a transition can be for as long as there have been armed transitions; what was missing was the handle. The *out* mark on the lane was drawn and deliberately not grabbable, which is the gap in one line. It is grabbable now on the same terms as the mix point — only once djmanzo is **holding** the mix, because `transition_adjust` refuses to move a proposal and a handle that does nothing is worse than no handle. **A resize, not a slide**: the start stays where it is, which is the same distinction `Deck::move_loop_edge` makes and the thing both the Rust test and the browser test assert. The arithmetic is Rust's, as §26's rule requires — the handle reports a *position* and `Transition::end_at` decides what length that is, rounding to a whole beat because nobody asks another DJ for a mix thirty-one and a half beats long, and clamping on `set_length`'s existing terms so an end dropped behind the start is the shortest mix rather than a mix running backwards. Adding it found the browser stub carrying the same defect its own comment warns about: it moved `end_frame` for a start nudge and not for a length change, so a shortened transition went on drawing its old end. **The beat jump's contextual action ships**, and it is the one item on §26's list that is not a drag — deliberately, because a beat jump has no position to grab: it is a move made to a record rather than a mark on one, which is why §26 gives it the other verb. A right-click on the lane offers the grid-aware moves, each one an action djmanzo already accepts, so a jump from the menu, from a controller, from a script and from the assistant are one thing in one log. **What makes it contextual rather than a fixed menu under another name**: a phrase jump is offered only when the record has phrase structure, and a move that would run off either end is not offered at all — so the menu is shorter near the top and near the end, which is the honest shape, and an unanalysed record gets no phrase entry rather than a jump by a guess. The arithmetic is Rust's, as §26's rule requires. **Driving the application found two things nothing else could.** The menu used `--panel-raised` on a waveform and was legible text on no visible menu — right for a panel on the page background, wrong for one on a record, and invisible in a booth. And it was drawn *inside* the lane, which is `overflow: hidden` and `contain: strict` for the transform that scrolls the strip, so it was **cut off at the waveform's edge**: five moves offered, two visible, looking exactly like a shorter menu. The test that now guards it was rewritten twice: `boundingBox` reports a layout box whether or not an ancestor clips it, `toBeVisible` knows nothing about clipping, and a trial click is worse than both — Playwright scrolls an element into view first, and an `overflow: hidden` box *is* programmatically scrollable, so the check reveals the entry it was meant to fail on. All three passed against the bug. What holds now is the invariant the fix establishes: nothing between the menu and the page cuts it off. What is left: **stem regions**. The reason filed against the first — *a stem region needs stem-aware analysis* — is now stale too: `dj_analysis::presence` is that analysis. What it still lacks is a *region* a DJ can grab, which is a design question rather than a missing measurement |
| 27 | Preview / ghost track | ✅ | **All seven of §27's questions are answered, and the list is a type rather than a memory.** `dj_app::ghost` answers *what happens if this record comes in here* for a candidate that is still a library row — the whole point of §27 is "before loading or committing" — and the geometry is `plan::plan`'s, unedited, so what is drawn beforehand is the mix djmanzo would actually perform. Nothing loads, nothing is armed, nothing is written down, and a browser test asserts exactly that. Beyond the planner it derives the three things inside the candidate the planner has no reason to know, each converted onto the **outgoing** record by the same multiplication — a beat of the candidate lasts an outgoing beat during the mix — and written once rather than three times, because three copies of one conversion are three marks that can diverge by a beat with nothing failing. *Where its first strong phrase would align* carries the lead-in beats with it, counted from the candidate's first sample rather than from its grid anchor. *Where the drop occurs* comes from §75's trajectory and is drawn as a mark on the ghost rather than as a number in a caption, because §27 asks for the drop as part of the **overlay** and a number beside one is a number about a place the eye then has to find. *Where the vocal enters* is the first window whose voice reading reaches `presence::STRONG`, drawn as a tick at the top of the ghost rather than a full-height line so it is not read as a second drop, and in the ghost's own colour rather than the loaded record's, because it is a claim about a record that is not loaded. An instrumental gets no mark, and neither does a voice the grid puts before the file starts. The overlay is §25's `suggestion` layer over the whole-record overview, which is where a ghost belongs and the scrolling lane is not: a mix point is minutes ahead of the playhead and would be off screen there until the decision was already made. **What makes this ✅ rather than seven ticks is that the seventh arrived without anyone editing a list.** `Asked::answered` is derived from `dj_render::layer`, so when `dj_analysis::presence` shipped and `vocal` stopped being `Drawn::Nowhere`, the count test failed and demanded a **frame** in `Ghost` — it was written as an alarm rather than as a tally, and it went off exactly as designed. The same mechanism puts a question back on the unseen list the day a layer is removed, and the panel's ability to say what it cannot see is tested on purpose rather than incidentally: with every question answered that line disappears, and an empty line and a broken one look identical from outside, so a browser test drives the unanswered state deliberately. One number here is **not** a measurement and says so: `presence::STRONG`, the share at which djmanzo will call a lead *carrying*, is the one stated guess in the chain — nothing in this container can hear a record, so it is a threshold to be tuned against real music rather than a figure anybody measured. An earlier reading of this section had it waiting on an audio preview; §27 says *display*, *overlay*, *ghost* |
| 28 | Stem-aware UI | ✅ | The stems module ships, folding so it costs a row when unused |
| 29 | Intelligent control handles | ✅ | **Progressive disclosure on a knob, with the table in Rust.** §29's last bullet — "MIDI = same underlying parameter" — is ADR-0003 stated as a control requirement, so `dj_app::handle` answers each gesture with **action text** rather than a number: a drag, a double-click, a menu entry and a MIDI CC all end up as the same action. Level one is the readout, which stays the interface's because it is about how a number reads. Level two is drag, **shift-drag at a quarter speed** (on the keyboard too), and **double-click to the parameter's own unity** — the one fact every call site used to spell out for itself, three EQ bands each naming `1` and the filter naming `0` with nothing making a fourth agree. Level three is a short contextual menu on **right-click or press-and-hold**, the second because a booth has trackpads and a control reachable only by right-click is one half the room cannot reach; it takes focus so Escape closes it, and every entry parses as an action, asserted by test. The menu is deliberately never more than four entries — §29's own warning is *do not turn every knob into a huge widget*, and a contextual menu is exactly where a knob grows into one. **And the AI hover ships, which was the last of the seven.** Its absence had a reason — the assistant stages whole *moves* rather than single parameter values — and §68's transition object made that reason stop being true: a held transition names a style, and `crate::shape` is the one table saying what a style does beyond the two channel faders, in the values the automix actually sends. So `handle::suggested` asks that table per control. **It answers about a mix djmanzo has planned, never in general**: a general opinion about where an EQ band should be is not a thing any software has, and a hover offering one would be the most confident invention in the interface. It is the held transition, only while it still describes what is on the decks, and empty for a deck the plan does not name. **Four of the six controls get nothing, every time**, and that is the answer rather than a gap: no style touches the mid band, the high band or the filter, and a mark on every knob saying "the assistant would leave this alone" is how a DJ learns to stop looking at the marks. What is left is the low band — the bass swap — the two channel faders, and sync on the deck coming in and *only* that one, because the record the room is already hearing is the tempo reference. The sentence is Rust's and the button sends an **action**, so §29's last bullet holds for the hover as well: a drag, a menu entry, a MIDI CC and now a hover all end up as one parameter. Driven under Xvfb with a 32-beat blend armed: deck 1's low knob reads *handed out of this deck by 50% through, then put back* and deck 2's reads *handed into*, the faders read *down* and *up over 32 beats*, and the mid and high knobs carry no mark at all. Deliberately not covered: the **gain trim**, which `autopilot::Step::MatchGain` really does set — it is a trim, not the channel fader, and a trim figure on the volume knob would be pointing at the wrong control while looking authoritative. Six controls are covered (three EQ bands, filter, volume, pitch), not every parameter in the registry: a table of four hundred entries is one nobody keeps true. **And the pads latch on all ten of their conditions now, which they did not.** `pads::Lit` is the same idea as the handles — a condition in a Rust table that the interface evaluates against the snapshot, so a new page is rows rather than cases in a component. Eight of the ten were evaluated; `StemMuted` and `StemSolo` returned `false` under a comment saying the pads would not latch *until the snapshot carries per-stem state*. It does, and it has for long enough that the stems **panel** has been drawing its mute buttons from the same two fields the whole time — so four pads a deck sat dark beside a panel showing exactly the state they were about. The Rust table was right, the snapshot was right, and the one line that reads them had gone stale. **Which stem is soloed is derived rather than carried**: the engine's solo mutes every stem and un-mutes exactly one, keeping the DJ's own pattern aside to restore on release, so the soloed stem is the audible one while a solo is held — a second field saying which would be a second answer that could disagree with the audio. The mute pads follow the live mutes, including the isolation a held solo imposes, because that is what the panel shows and two surfaces for one state must not disagree |
| 30 | Colour system named for meaning | ✅ | **All fourteen roles are colours the interface draws.** Each is an *alias* onto a token the palettes already define, so all seven packages get all fourteen and nothing keeps a hundred and sixty-eight numbers true — and a palette that wants its own may override any of them. The rule is `Role::must_differ_from`'s, and a Rust test reads `app.css` and fails on three things: a role with no value, a must-differ pair collapsed onto one token, and a role nothing asks for. That last one is why this row moved: the roles were types with tests and the stylesheet went on using the appearance tokens. Roles outside a pair may share — `active`, `incoming` and `success` are all the accent, and a control being on is never mistaken for a record arriving, which is §30's own point about not forcing fourteen arbitrary hues. **The conversion found a second defect**: components asked for `--muted` forty-seven times, `--ok` fourteen, `--line` ten and `--control` eight, and none of those had a value anywhere — the ones without a fallback inherited (dim text was not dim) and the ones with one drew a fixed hue on every palette, escaping the hex-literal test through its single exemption. All of them are defined now, and a vitest asserts every token a component asks for is provided by somebody. **And the roles are now asked for, which they were not.** They existed and almost nothing used them: every control painted its *this is on* state with `--accent` or `--accent-2` directly, which renders identically today — `--active` **is** the accent — and threw the meaning away. Thirty rules across twenty-five components have been swept onto the role they mean, and the split is the useful half: a control the DJ **chose** is `--selected`, one that is **running** is `--active`, and `must_differ_from` has named those two as a pair that must stay apart since §30 was written while in thirty places they were one colour. Two rules were serving both meanings at once and are split — the browser's view switch against §76's AI lens, and a pinned candidate against a ghosting one — because turning the lens on looked exactly like picking a third view, which §76 says it is not. Two roles gained the partner a fill needs: every filled state paired its background with `--on-accent`, including the ones filled with `--accent-2`, and read well on all seven palettes by luck rather than by construction. A vitest holds both rules — a state selector that paints must name a role, and a state filled with a role must name that role's own text colour — and it found two more the sweep had missed |
| 31 | Theme adaptation | 🟡 | **Mostly brakes, which is what §31 is.** "Never allow the interface to flicker from color to color every time the track changes" is the feature; choosing a theme from a reading is the easy half. `dj_app::mood` holds all five stabilisers §31 lists: a **four-minute minimum** (a record is three to six, so a per-track flicker is impossible by construction), **forty seconds of hysteresis** before a new answer is believed, a **three-second fade** on the change, a **duration** the caller can override, and a **manual lock** that beats all of them — a DJ who has chosen has decided, and choosing from the menu tells djmanzo to stop deciding. A four-hour test of readings that flip every three minutes moves the theme a handful of times. **Venue ambience is §81's setting**, told rather than sensed: there is no light sensor here, and §31's own examples are *beach*, *club* and *daylight* — the same axis §81 already asks about, which is why it came first. Musical context and phase collapse into `SessionPhase` on purpose: it is read from energy and tempo over minutes, where the genre of the record on deck 1 changes every four. The decision is Rust's and the pixels are the interface's, the same split the density bands use, and a test reads the shipped theme ids out of `packages.ts` so djmanzo cannot ask for a theme nothing can wear. What §31 lists and this does not have: **daylight as a measurement** — whether the sun is on the screen right now. A DJ can choose that theme; djmanzo cannot notice they should |
| 32 | Theme packs | ✅ | **The architecture was there; the table was not.** `ui/src/controls/themes/` has been a real pack architecture for a long time — a package is a palette, a geometry generator, behaviours and effects, through one pipeline — and what was missing is anything that knew *which themes there are supposed to be*. Rust named theme ids in three places (§31's mood table, §7's arrangements, §54's setups) and each had its own test that read `packages.ts` and grepped for `id: "…"`: three copies of one guard, all asking *does this id exist*. None could answer what §32 asks. `dj_app::theme` is that table now, and the guard is asked once and **in both directions** — a package the interface ships and nothing names is a failure too, and was invisible before. **A row that does not ship says why**, the posture §8 takes about what djmanzo remembers, and the picker shows those rows: six of §32's sixteen ship, ten do not, and each of the ten carries its reason. Two of them are decisions rather than gaps — *High Contrast* is an override the stylesheet already applies over every theme from the operating system's own setting, which is stronger than a seventeenth palette a DJ has to find, and *Minimal* is a density, which §5 already fits to the window; a theme by that name would be a second control for one thing. **And the watershed is a theme now.** §32 is explicit that the metaphor should become a pack and *must not constrain DJs who do not want metaphors*: it was a switch in the status strip beside the themes rather than one of them, which made it a mode — the thing §32 says it should stop being. `pkg-watershed` ships with a palette keyed to the water the renderer actually draws, and choosing it opens the world; the switch still closes it and no other theme touches it, so both halves of §32's sentence hold. **`theme_chosen` refuses an id nothing ships**, which it could not do before this table: `applyPackagePalette` falls back to the organic palette, so a typo was worn silently as another theme's colours with nothing thrown. **And the eight palettes are designed.** Club (blue-violet, deliberately not Industrial Techno's near-black: a DJ who asked for a club theme should not be handed hard music's), Festival (warm and high-chroma, the main-stage half Daylight does not cover), Caribbean (whose light variant is the one that matters — a beach bar at four in the afternoon), Latin, Wedding (low chroma and warm neutral, because the guests can see your screen), Lounge (the dimmest of the set; a lounge screen that announced itself would be doing the opposite of its job), Scratch (near-monochrome, so the platter marker and the cue are the only things carrying colour) and Stem Lab. Sixteen of §32's eighteen rows now ship a palette; the two that do not are the two that deliberately are not palettes, and their reasons are unchanged. **Designing them found a real defect in the nine that already shipped.** `Role::must_differ_from` names the pairs a DJ has to tell apart and a test checks that each pair points at *different tokens* — necessary, not sufficient. In the organic palette's light variant `--accent` was `#0f7b57` and `--accent-2` was `#057a5f`: two token names and one green. So *selected* and *active*, *the assistant did it* and *the room did it*, and the vocal and other stems were each one colour, in a theme that had passed every test in the file for months. `the_pairs_that_must_differ_differ_to_the_eye_in_every_palette` measures CIE76 ΔE in Lab — a WCAG ratio answers the wrong question, since red and blue have nearly the same luminance — and holds every pair at 20, which the palettes that were right already clear comfortably. Five themes were repaired: Organic, Studio, Booth, Sunset and Watershed |
| 33 | Accessibility | 🟡 | **The audit has been run, and it found eight kinds of defect.** axe-core (MPL-2.0, dev-only, no dependencies) over the shell and every panel the rail opens, now permanent as `ui/e2e/access.spec.ts`. The row above was true and was also the reason nobody had looked: querying by role proves a control *has* one, and proves nothing about whether the role may carry the label written on it or whether the text can be read. What it found, two of which had shipped since the components were written: every deck's **level meter announcing itself as an empty box** (ARIA drops `aria-label` from an element with no role, so the words reached nobody); every deck's **position bar announcing a percentage of nothing**; the label of **whichever panel button the pointer was on** at 1.23:1 — that is, the panel you had just opened; the mission bar's captions at 3.39:1, under a section that asks for *daylight*; the **chosen posture** at 2.4:1 and the **chosen arc** at 1.47:1, both from a component restating half of a colour pair; the browser's two tabs reporting an attribute their role forbids; a table column whose cells belonged to no heading; and no `h1` anywhere, so the whole booth was one flat run of controls. All fixed. **Colour alone never encodes critical state**: §33 states that as an absolute and axe cannot check it, so it is a type — `mission::Level::mark` — and the mission bar now carries three channels where it carried one, the colour plus a mark plus an outline that becomes a fill. Two Rust tests keep the marks and the stylesheet honest about it. **High contrast** is the system's setting, honoured as a media query so all seven theme packages get it. **Reduced motion** was already honoured twice over, including §18's own `motion: none`. **Keyboard operation**: a panel opens from the keyboard and it is the one that had focus. What is left, and why the row is still amber: axe is a rules engine, so a clean run is a floor rather than a verdict — it cannot say whether a label is *useful*, whether the tab order makes sense to somebody mixing, or whether a colour-blind DJ can tell an armed deck from a playing one. **A colour-blind-safe palette ships now.** The rule that no *state* is carried by colour alone was already there and is not the same promise, because the four stems are the exception it cannot cover: they are four things read as a set, and four labels in the space four swatches occupy is not a design. So every shipped palette was put through protanopia, deuteranopia and tritanopia simulations (Machado, Oliveira and Fernandes 2009, at full severity) and **every one of them collapses somewhere** — the organic palette's *selected* and *active* land 1.6 apart under deuteranopia, the watershed's 1.8, cyber's 1.8, which is one colour to roughly one man in twelve. They are not all fixable: four hues that survive all three dichromacies are not a palette anybody picks for a room, and forcing every theme into that shape would be deleting the themes. What §33 asks is that a DJ who needs one *has* one, so `pkg-legible` is a theme of its own — amber, pale yellow, red and blue in the dark; blue, olive, crimson and rust in the light — whose worst must-differ pair is 27 apart under every simulation, held there by `at_least_one_palette_holds_up_for_a_colour_blind_dj`. What is left, and why the row is still amber: **nothing here has been in front of a screen reader**, and axe is a rules engine — a clean run is a floor rather than a verdict, and it cannot say whether a label is *useful* or whether the tab order makes sense to somebody mixing. That gap does not close in a container |
| 34 | Crowd / audience intelligence | 🟡 | RoomSense and audience requests ship, and with §35 and §37 the readings are now placed against the night's own reaches and correlated with what the DJ did. What is not built is the rest of §34's list — nothing identifies who is in the room, and nothing should |
| 35 | Room baseline | 🟡 | **All four of §35's comparisons ship, and no absolute threshold anywhere.** `dj_assistant::room::Horizon` is the reach a reading is placed against and `Baseline` is the placing: the middle of the last three minutes (§35's *current*), against the last twenty minutes (*recent*), against the whole night (*earlier*), and against **every other stretch of tonight that was at this phase** — the fourth, and the only one that is not simply a longer window, because a peak an hour ago is a better comparison for a peak now than the cooldown in between. Every answer is a position inside a distribution this room produced tonight, so nothing means anything absolute and nothing needs a calibrated lens; that is §35's opening refusal expressed as a type rather than promised in a comment. The phase reach carries the guard that matters: a reading is filed under the phase the night was in *at the time*, so the near window sits inside the phase's own distribution, and `ENOUGH_AT_PHASE` refuses the comparison until three quarters of it is some other stretch — without it a night that reached peak two minutes ago would confidently report the room as usual for a peak. Shorter reaches earn a sentence only by **disagreeing** with the night: *quieter than it has been tonight, busier than the last twenty minutes* is a room coming back, and three sentences saying a floor emptied is a panel nobody finishes. Drawn as a table under the meters, one row per sense, a reach it cannot make shown as absent rather than as "usual". What §35 also asks for and this does not have: the **venue** half of "venue/session baseline" — comparing tonight with previous nights in the same room needs a stored reading series, and `nights` carries no sensor history. **Nothing here has been shown to work on a real camera or microphone:** this container has neither, so it is tested against synthetic series in Rust and a fixture in the browser, and it claims nothing about how a room reads |
| 36 | Multi-signal crowd model | 🟡 | Light, movement, loudness and time of day ship, each placed against §35's four reaches rather than against a threshold. What §36 asks for beyond the signals — combining them into one model of the crowd — is not built, and is deliberately not faked: three numbers that disagree are three facts, and averaging them into a mood is the statistics-lying this module exists to avoid |
| 37 | Causal crowd analysis | 🟡 | **The correlation ships, and it is never called a cause.** `dj_app::response` reads what the room did across a mix — the minute before it began against the **twelve to thirty seconds after it ended**, §37's own window — and `dj_app::mixes` supplies the mixes, so the DJ action half is the action log rather than a second detector. A median over each window, so one frame where somebody crossed the lens is not a finding, and a window that cannot reach five readings answers nothing rather than thinly. Across nights it is a tally with §81's discipline at a stricter setting: **three nights minimum, two thirds must agree**, a night is one vote however many mixes it made, and a night that did both across its own mixes abstains rather than being resolved to whichever came last. The sentence always names the evidence and the kind of night — *"After a blend at a club night, the floor has picked up — 4 of 5 nights"* — and never says *because*: a floor that fills twelve seconds after a mix may be filling because of it, because the last record ended, or because it was going to anyway, and nothing djmanzo has can tell those apart. **This is the one thing djmanzo writes down about a room**, in `mix_responses` (migration 13), because the readings live twenty minutes and a night's log does not outlive the run — the same exception §81 took, taken as narrowly: one row per mix per sense, four values, no time series. What §37 asks for and this does not have: *"here"* is a **kind of night** rather than a venue, because djmanzo has no venue and inventing one would be inventing a fact; and the chain is one action deep — a transition — rather than any DJ action. **Nothing here has been shown to work on a real camera or microphone:** this container has neither, so nothing has ever been recorded in it, and the panel is correct and empty |
| 38 | Crowd signals never control the DJ unasked | ⚖️ | |
| 39 | UI for audience intelligence | 🟡 | **The compact indicator ships, and the dashboard is a surface of its own.** §39 is a warning before it is a feature — *do not create a giant analytics dashboard during a set* — so what sits in the status strip is one mark: `ROOM ↑`, `ROOM ↓` or `ROOM STABLE`, with the evidence as a count (*2 of 3 senses*) rather than as a percentage, which is the same shape §37's nights are reported in. `Room::glance` is the judgement, made once and read by both the chip and the panel, and it is placed against the **last twenty minutes alone**: the question a DJ mid-mix asks is whether the floor is picking up now, and the night's reach answers a different one. The senses are counted, never averaged — §36's refusal — so a floor that got busier while the room got quieter reads STABLE with agreement well below its evidence and names both halves. **An em dash rather than STABLE when nothing can be placed**, which is the state of every machine with no camera. RoomSense was a `<details>` fold two levels inside the assistant and is now the `room` surface, removed from the assistant rather than duplicated. What §39 lists and this does not have: *recent transitions* and *response timing* are §37's and are drawn in the same panel, but **the chip goes quiet a few minutes after the panel is closed** — the camera lives with the panel, on purpose, and djmanzo stops claiming to know once it stops looking |
| 40 | Assistant sees everything important | 🟡 | **The assistant was blind and is not any more.** `ask` handed the model a system prompt, the action vocabulary and the DJ's sentence — and nothing else. Not what was playing, not the tempo, not the key, not where either record was. *"Bring the next one in on the phrase"* was answered by something that could not see a phrase, and the only reason that ever looked like it worked is that most of what a DJ types has the answer already in the sentence. `dj_app::sight` is §40's own list of twenty-six, in §40's order and words, each with what carries it — and `brief` turns the snapshot the interface is already being sent into the lines the model is given, so what it is told and what the DJ is looking at cannot disagree. Nineteen of the twenty-six are gathered: what is on each deck, the positions, tempo, beat grid, key, phrase, stems, cue points, loop, effects, the crossfader, the night’s energy and phase, the posture and the occasion — and four that are not on the snapshot at all and are read beside it: **tonight’s mixes** (derived from the action log by `mixes::handovers`, the same reading the Mixes panel draws, so the two cannot disagree), **the last twelve actions** in the action vocabulary’s own words, **§53’s controller profile** as counts rather than a mapping file, and **the arrangement on screen**. A list arrives as its recent end and says so — *(the last 12 of 40)* — because a six-hour night’s log would spend the context window on the warm-up, and a briefing silently cut reads as a night with twelve actions in it. Injected the way §41's interface vocabulary is, so `dj_assistant` still has never heard of a deck, and nothing about it widens what the assistant may *do* — every action coming back is parsed and validated exactly as before. **The seven it cannot see are on screen with the reason beside each.** That is the half worth having: an answer that ignored your history reads very differently once you know the thing answering could not see it, and a panel listing only what the assistant knows is a reassurance rather than a fact. A test resolves every gathered item's pointer against a real capture, so a table promising the assistant can see the cue points through a field that does not exist fails rather than reassures. **Twenty-four of the twenty-six now, and five of the seven absences were reasons that had quietly stopped being true.** *The rail is computed on demand and is not on the snapshot* and *the plan is the planner's* were both accurate, and neither was a reason the assistant could not be **told**: a briefing is assembled when a DJ types a question rather than sixty times a second, so the cost is one ranking per question — the same one the rail already pays — and reading one more lock is what the caller is for. So the assistant now sees **what it has staged** (§44's transaction, in the words the panel puts in front of the DJ — an assistant asked "should I bring it in" that cannot see it already prepared the record is answering a different question from the one on screen), **the rail's next five with their reasons** (answering "what should I play next" without seeing what djmanzo already offered is two systems disagreeing in front of a DJ), **the planned set with the next one marked**, **§81's profile as the one sentence Rust writes** rather than the weights behind it, and **§68's armed transition as a shape and a length** — that last one unseen on the reasoning that *a transition happens over time and the briefing is one instant*, which stopped being true when a mix became something djmanzo holds. The rail and the deck it follows use `context::carrying`, so the tempo the model is told and the candidates it is shown are about one record. What is left: the **library**, which stays out by design — a collection is thousands of records and a briefing carrying it would be the database in a prompt, and searching is what a search is for — and the **room**, whose reading lives with the panel that opens the camera and stops when it closes, so a briefing carrying it would go on claiming a room nothing is looking at. That second one is solvable where there *is* a camera, by carrying it only while something is watching; this container has none, so it is named rather than guessed at. `SessionContext` also carries the phase, its certainty, its basis and the drift, and the autopilot reads the certainty; §11's `musicContext`, `hardwareContext` and `djBehaviorContext` are gathered now — see §11 — which the briefing does not yet read: what the assistant is told and what the context engine holds are two lists that have not been joined, and joining them is the next thing this row asks for rather than a gap in either |
| 41 | AI can operate the GUI indirectly | ✅ | `dj_app::uiop` — `ui show prepare`, `ui pin room`, `ui focus 2`, generated from `cockpit::surfaces()` so a panel djmanzo does not have cannot be asked for. Gated by §72's `adapt_layout` row, applied in Rust and announced on an event, because a panel the assistant opened has to appear without anybody pressing anything. Reachable by hand from the palette too. Density is deliberately excluded: the interface measures its own |
| 42 | Suggestions must be explainable | ✅ | The transition planner states where and how, with its reasoning |
| 43 | Suggestion fatigue | ✅ | **The rule, the number, and the sentence that stops it reading as a bug.** `dj_assistant::fatigue` counts what the rail offered and what the DJ played: twenty consecutive records that djmanzo did not suggest and the rate falls a step, again at forty, again at sixty, and one suggested record played puts it straight back to full. §43 says *consecutive*, so that reset is the rule rather than a kindness — staying quiet after a hit punishes the DJ for the machine's earlier noise. Three decisions carry the section. **Only a load resolves an offer**: the rail is polled, so counting each answer would reach twenty in about a minute of nobody doing anything and the assistant would go quiet at a DJ who had never looked at it; twenty ignored suggestions means twenty records *played*, which is an hour of a set and a real opinion. **A load with nothing standing is neither taken nor ignored**, so a DJ who works entirely from their own crates is not mistaken for one rejecting advice. And **the floor still offers one**: a rail that offers nothing can never be taken from, so the streak could never reset and the assistant would be off for the night with no way back. The hook is in `put_on_deck`, djmanzo's one load funnel, so a record dragged in, picked from a crate, sent by a controller, asked for by the assistant or brought by the automix all count the same — §87's *one source of state truth*, used. **It does not move the posture.** §43 lists six states and djmanzo has six, but those are the DJ's own setting and a machine that quietly demoted Suggest to Watch because it felt unwanted would be deciding something that is not its to decide; what falls is the rate. **And it says so**, in the assistant panel, always — the obvious implementation of *do not spam* is to go quiet and say nothing, and a DJ whose rail has thinned would then have no way to tell whether djmanzo had taken the hint, crashed, or run out of library. The line carries the count, the way back, and §33's non-colour mark. §18's budget is now read too, and deliberately only to **silence the rail in an emergency**: a rail that dropped from eight rows to one the moment a second deck became audible would look broken on a panel the DJ opened and is looking at, which is the failure §17 avoided by gating on `reflow` rather than second-guessing an open panel — but a failed recording is §18's own *the DJ needs the controls, not the advice*, and there the rail goes. Not persisted: fatigue is a fact about tonight, and a DJ who worked from their crates last Saturday should not open djmanzo on a Tuesday to a rail that has already given up on them |
| 44 | Transactional AI actions | ✅ | `dj_app::staged`. The whole next transition — load, cue, trim, sync, mix — staged as one thing with Accept, Modify and Reject, in a strip under the top bar rather than a panel. Accepting runs every chosen move through the same `perform_step` the automatic tick uses, so there is no second execution path and the whole thing logs and replays. Partial success is reported as partial |
| 45 | Instant manual takeover | ✅ | Per parameter. Touching a control wins |
| 46 | Guardrails for autopilot | ✅ | Careful mode holds the controls that cannot be undone by pressing them again |
| 47 | Emergency UX | ✅ | **SAFE**, beside REC and Mark, and `safe` on the action bus so it is also on a controller and in a script. Takes every control back, drops anything staged, clears every rack, flattens the EQ and filter, restores master gain and the limiter. It never stops a record, moves a fader or touches the crossfader — asserted over the text of every action it expands into, because what it refuses to do is the part that matters |
| 48 | Performance / laptop mode | ✅ | **The detection was there; the priority was not.** `performance.svelte.ts` has measured the interface's own frame rate for a long time — down a tier on a bad second, up after ten good ones — and what consulted that tier was the theme pipeline and nothing else: two of §48's seven bullets, both in the cheapest band. So the machine was detected, almost nothing was given up, and **the sentence §48 ends on had no expression anywhere**. `thrift::Spend` is §48's seven as a table with a band each, and `Band` is `AUDIO > CONTROL > VISUAL EFFECTS` as a type whose derived ordering *is* the priority. Three tests hold it: nothing in the audio band is ever given up, nothing cheaper is kept while something dearer goes, and a harder-pressed machine never pays for more. That is not ceremony — the failure §48 forbids is the one that ends a set, a laptop under load that keeps its glow and drops its sound, and it is exactly the failure a well-meaning edit produces, because the audio band is the only one where giving something up would buy much. **One saving is newly real**: §48 names *reduce audience polling frequency*, and the room read — a camera frame scaled down and optical-flowed against the last one — now runs every eight seconds at Eco instead of two. Twenty-two readings still fill §39's three-minute near window, which is the test of whether a saving costs a feature or only sharpness. The number lives beside the priority table and a test fails if the panel grows its own again. **Three of the seven are kept and say why**, which is §8's posture: the audio is a claim the architecture makes rather than an intention (ADR-0001 — the audio thread never allocates, locks or does I/O, and nothing the interface decides can reach it); §25's *visual layers* are a DJ's choice that §79 lets them lock, so djmanzo drops the **answering** rather than the layers; and there are no expensive previews to stop drawing, the overview being a cached tile. The whole list is on screen under Performance, because a DJ who can see a tier name and nothing about what it cost them cannot trust any of this. What is left for this row: nothing this container can build — the remaining savings are ones djmanzo does not currently pay for |
| 49 | Professional workflow principle | ⚖️ | |
| 50 | Don't over-modalize | ✅ | The dock manager is this section: panels stopped taking turns |
| 51 | Command palette | ✅ | `Ctrl/Cmd + K`, assembled in Rust from `dj_core::vocabulary` and the cockpit's own surfaces rather than a written list, so it cannot offer a command djmanzo does not have. **What you type is an entry**: `deck 2 loop 8` parses, so the top row runs it — which is the only way the verbs taking an argument are reachable, and what §51 means by "the semantic interface exposed to voice/AI" |
| 52 | Hardware-first thinking | ⚖️ | The pad zone is a page strip and eight pads because that is what hardware has |
| 53 | Controller-aware GUI | 🟡 | **The interface reads what is plugged in now.** `dj_hid::hands::Hands` answers six of §53's eight questions off the *mapping* rather than off the device — a MIDI controller announces a name and nothing else, and its bindings are what a hand will actually find. That is also the right answer rather than a workaround: the shipped DDJ-SR mapping reads as **two** decks though the file's own header says the hardware splits across four MIDI channels, because two is what that mapping binds, and counting the channels the vendor wired would tell the interface to make room for two decks nobody can reach. A test holds every bundled mapping to a sane profile. **§53's worked example is the first of two judgements it makes**: a controller that cannot reach the stems unfolds the stem module — the biggest block on a deck, about 370 px — and one with stem pads leaves it folded, because then the module is a readout and the hand is elsewhere. *Nothing plugged in* is deliberately a third answer and leaves it folded: a laptop-only DJ is the case the interface is already designed around, and unfolding for them would be §53 answering a question nobody asked. **And the lights now go out.** §53's list ends with *LED feedback*, and the return path was the fourth table this branch has found parsed, validated and consulted by nothing: a mapping declared its `[[feedback]]` blocks, `FeedbackMap::parse` resolved every parameter name in them, `Feedback` worked out which byte each wanted and which had changed — and no byte ever left the machine. `feedback::Lights` is the thing that sends them: its own thread, because a blocked MIDI write must not be able to stall the interface and a slow interface must not be able to leave a play button lit on a deck that stopped; the registry is the shared truth between them and is lock-free, so it costs one atomic read per light per tick. Thirty times a second rather than the snapshot rate, because a DIN cable carries about a thousand three-byte messages a second and a busy board at 60 Hz would be twice the budget — and only what *changed* is sent, so a still mixer costs nothing at all. **It goes dark when it is dropped**, in `Drop` rather than in a close path, because the cases that matter are the ones nobody writes a close path for: a different mapping chosen, a device unplugged, a quit. A controller keeps its LEDs after the thing that set them has let go — they are the device's state, not djmanzo's. All three are read out of the mapping file **in one pass at load**, beside §53's hands profile, because a `Mapping` carries neither and a set assembled from three reads could be half of one mapping and half of another; a file whose lights do not parse is refused whole, since the alternative is a controller that works perfectly and never lights up with nothing saying why. The panel now says which of the three kinds of dark a dark board is — driven, described but unsendable with the reason, or not described — because "nothing lit" is the same picture for all three and only one of them is something a DJ can do anything about. **The second judgement is the platter**, and it is the same shape pointed at a different gap. The deck's wheel is a small readout on purpose — the waveform above answers *where am I* better than a circle does, and a full row for it cost more than the waveform got in three of the four shipped arrangements — but that reasoning assumes the hand has somewhere better to be. A pad-and-fader controller puts the hands on the pads, the faders and the EQ and leaves nudging a record back into time as the one gesture on that deck with no hardware behind it, so the wheel grows by half. **It only ever grows**, which is a rule rather than an omission: a controller *with* a platter makes the screen's circle a readout and the tempting symmetry is to shrink it and give the pixels to the waveform, but that is contextual demotion, which §3 refuses and §17 is built never to do — nothing a DJ can see is taken away by a piece of hardware being plugged in. Nothing plugged in leaves it alone too, on the stem rule's own terms. **And a stated size outranks it.** §5B's scratch composition asks for about 200 px and means it; the adaptation moves a *default*, so a DJ who asked for a platter and then plugged in a pad controller to go with it does not watch it halve. That precedence had nothing testing it until a mutation went looking — every other case opens the shipped deck, and §5B's own tests open with no controller, so the two conditions had never met. Measured as rendered geometry rather than as a flag, and as a comparison rather than against a number, because every size on a deck is multiplied by the density token: the first version of that assertion demanded more than 90 px of a wheel that renders at 83, with the adaptation working perfectly. What is left: the screens, which are driven by a controller's own firmware and are unknowable from a mapping — said in a sentence rather than counted, and the two `pub const`s that used to hold that sentence and its LED twin are gone, because neither was read by anything and one of them had been claiming since `feedback::Lights` shipped that djmanzo does not drive lights. **This container has no MIDI service at all** — the panel says so in as many words, and it is the MIDI service rather than an empty device list — so the adaptation and the lights are both proven by the Rust tests: over every shipped mapping for the reading, and against a recording sink rather than hardware for the pump. Nothing here has watched an LED come on, and nothing claims to. What the running application shows is the no-controller case behaving as designed |
| 54 | Professional functional presets | ✅ | **The functional ones ship now.** §54 asks for presets that configure *many* systems at once; djmanzo had presets that configured one — §7's workspaces set the cockpit and stop at the edge of the screen, and everything else §54 lists is a control somewhere else. `setup::Setup` is six presets, one per kind of night, and each names an arrangement, a theme, §25's waveform layers, the pad pages, the assistant's posture and whether the room may ask for records. **The six are §81's, not a second list.** §54's own example is *"Beach Sunset / Latin Resort"*, which is an occasion rather than a layout, and `setting::Setting` is already that list — told rather than guessed, already read by the genre learning and by §31's theme. Inventing a second would be two names for one thing, and the stale one would be the one a DJ picked from. **Reading a preset is a separate act from applying it.** The first press lists all six changes in the DJ's own words; the second does them. `dj_app::presets` states the rule for the small presets — *a preset that silently changes eight things is the kind of feature people stop trusting* — and §54's change more than that. Rust does what Rust owns and hands back the arrangement and the theme, because a cockpit is resolved against a window only the interface has measured. **Two of §54's list were named as absent rather than faked, and both formats now exist**: technique and genre packs were §16 and §32, neither shipped as a format, and there was nothing a preset could name. `dj_assistant::pack` and `dj_app::theme` are those formats, so this is a wiring job rather than a blocked one — a preset can name a knowledge pack the way it already names a theme. The third stays absent on purpose: session-phase weights would be a preset telling the context engine what to read, which is the wrong direction — §11 derives the phase from the music, and a wedding preset pre-weighting it towards warm-up would be djmanzo deciding the night before hearing it. **And a preset now names a knowledge pack** — derived rather than written down, because every pack already names the occasion it pairs with and a column here would be that claim twice. Three of the six get one; the club gets none, because house and techno both name it and picking between them would be djmanzo deciding what kind of club night this is. What is left for this row: nothing that this container can build. §54's own list is covered, and the remaining §16 packs and §32 palettes are content for their own sections |
| 55 | Visual language architecture | ✅ | ADR-0009 and a validated token set |
| 56 | Visual feedback should be functional | ⚖️ | |
| 57 | Waveform colour must be semantic | ✅ | Every drawn layer declares what its colour *means*, and a test refuses two unrelated layers on one meaning — a property of the set, which is why the set exists. Grouped roles are named and counted: the three grid layers are one meaning at three weights, which is texture rather than a second colour. An unbuilt layer reserves nothing |
| 58 | Information hierarchy (tiers) | ✅ | **Stale as 🟡 on both halves.** The tiers were *not* modelled — there was no type, only the words in §18's doc comment — and the rail that would use them, §74's, has shipped since. `tiers::Tier` is the four now, with every verb of the action vocabulary and every dockable surface placed in one, and a test walks both lists so a verb added to `dj_core::vocabulary` and left unplaced fails rather than defaulting quietly: the failure mode of a default is the worst one available, because the new verb is the one a DJ has not learned the position of and it would be the one buried. **What reads it is the command palette**, which is where §98 says a whole night's work should be reachable from. It answers a query with every matching verb, surface and interface operation and then cuts to twelve; the order was whichever order the passes generated in, so a DJ who typed three letters mid-mix could be offered *Pin the Journal* above *deck 2 cue*. Ranked by tier now, stably, before the cut — and the ordering is drawn as a line down the left edge of each row, so the boundary between the hands, what djmanzo has to say, and the paperwork is visible rather than merely true. §18's own sentence becomes a rule with it: `Attention::performing` has said *Tier 1 and 2 only; nothing else may take room* since it was written, and `Tier::survives_a_mix` is the first thing that can be checked against it. **The gap this row used to name was based on a wrong premise, and finding that out was the work.** It said the tiers did not yet gate what a phase may promote. They do not, and they should not: every budget whose `room_for` is narrower than tier 4 also has `reflow: false`, so a tier filter on §17's priorities would be a rule that can never fire — a rule about nothing, which is the thing this codebase keeps finding and deleting. Nor should the phase lists be *sorted* by tier: §17's own order inside a phase is deliberate (setup checks the sound card first), and re-ranking it would throw that away the way §58's palette deliberately does not re-rank inside a tier. What the budget gates instead is the palette, under §18 above. What is still not here: §26's remaining handles, and the tiers say nothing about §74's rail, which judges its five by what the hands are doing rather than by rank |
| 59 | Density system | ✅ | Five bands, and the fixed-pixel blocks that ignored them are fixed — density moves a deck 122 px now, against 68 before |
| 60 | Resizability | ✅ | The band follows the window, with a test at five heights |
| 61 | Phone as a secondary surface | ✅ | Room sensor and audience requests over LAN |
| 62 | Audience camera privacy | ⚖️ | Local only; nothing leaves the machine |
| 63 | AI privacy | ⚖️ | Local models, a spend cap, and the budget shown |
| 64 | Competitor lessons | ✅ | Researched live and recorded in the audit with sources |
| 65 | Community research principle | ✅ | DJ forums are blocked at the egress gateway; web search was used instead and that limitation is written down |
| 66 | DJ workflow knowledge | ✅ | The workflow model is audit §9 |
| 67 | The session is a loop, not a screen | 🟡 | The action log has been the session since M0, and it now *contains transitions* the way §67 lists them: `dj_app::mixes` reads a night back as the mixes in it — what went into what, over how many beats, and what kind of mix each was — derived from the log rather than recorded beside it, so a set recorded before any of this has them too. **Tonight's mixes** is the surface. **And two more of §67's fourteen are real now: *AI interventions* and *manual interventions*.** The log could not tell them apart — a crossfader move from a finger, a controller, the autopilot and an accepted transaction all arrived at `ActionBus::dispatch` looking identical, so "what did djmanzo do tonight" and "what did I do" were the same question with one answer. `dj_control::By` is that answer: every path that knows it is djmanzo acting goes through `dispatch_by`/`perform_by` with `By::Machine`, and everything else keeps the plain call and is a person's, which is the safer default — an action wrongly filed as the DJ's makes djmanzo *more* deferential, and one wrongly filed as the machine's would let it claim a move a person made. In a session file it is a `*` before the action, so a take written before any of this reads as all a person's, which is exactly what it is; no version bump, and old files read correctly rather than merely parsing. **Writing it down found a defect in §87.** `note_human_touch` — the takeover, "a person touched this, so leave it alone" — fired for *every* action through `perform`, which is the same path the automix and the autopilot use on purpose. So djmanzo marked its own fader move as the DJ's, `Takeover::may_move` refused that control for ten minutes, and `autopilot::next_step` declines a held one: the machine was handing itself the controls it had just used, once per action, for the length of every automix blend. Gated on `By::Hand` now, with a test in both directions — a DJ's hand must still take a control instantly, and a fix that stopped noticing that would be worse than the bug. **And the list is gathered now**, which is what this row named as the gap. `parts::Part` is §67's fourteen, each with the module that owns it, how it reaches the interface, and — for the one djmanzo refuses — why it does not. **Deliberately a table rather than a `Session` struct**: thirteen of the fourteen already have an owner that decides them, and a second copy gathered into one object is the failure this codebase has found six times from the other side. §11 met the same problem and answered it the same way — `DJContext` is gathered in one pass rather than stored twice, and what makes it real is a table held against the code. **The claim being tested is §67's own last line**: *the GUI is simply a live window into that session*. So each part says how it is seen, and the two kinds are kept apart because only one is live: the phase and what is on a deck ride the sixty-a-second snapshot, and eleven others are commands a panel calls when it needs the answer — a transition plan changes twice a record, and putting it on the pump would be the furniture §90 keeps taking off. A JSON pointer is resolved against a real capture, the way §40's are; a command name is checked against `commands.rs`. **Writing it down was the work, and three rows were wrong.** *Room state* pointed at `audience.rs`, which is the room's **requests** — the page a stranger types into — and not the room's state at all; two things called audience is exactly how a row lands somewhere plausible and wrong. *Notes* pointed at `memory.rs`, which is *finding a record from what you remember of it*: a search, not a notebook. And three command names were recollection and did not exist. Every one read as an answer until a test resolved it. What is left for this row: the **set arc**, and it is the same refusal §11's row already makes rather than a second gap — a set arc is a statement about a stretch of time and a phase is a reading of one minute. A test asserts it is the only part with no window, and that its refusal says what djmanzo does instead, so the two rows cannot drift apart |
| 68 | Transition object | ✅ | `dj_app::transition::Transition` carries every field §68 lists: the two decks, where the mix starts and ends in frames and seconds, its length, style, tempo delta, key relation, the pair's confidence on the rail's scale, the typed reasons, and now the **stem, EQ and FX plans**. Those four come from `dj_app::shape`, which is one table saying what a style does beyond the faders — and it is the table **the automix performs**, so what the pair view says a style will do before it is pressed is what happens when it is. They are derived from the style rather than stored on the object, because a stored copy is a second answer that a restyle can leave behind. djmanzo **holds** a transition; it can be moved on the waveform, shortened and restyled, and an edit **re-derives the reasons over the new geometry** rather than keeping the planner's. **The automix performs it** — its decks, its start, its length, its style — instead of deciding its own, and the autopilot defers to it rather than pushing settings in; where it does not apply it is ignored rather than forced. The object exists on **both** sides of a mix: `dj_app::mixes` derives the performed one out of the log, and it **drives replay** — `replay::Window` renders one handover back to a WAV with its run-up, from the set the DJ is playing. Everything before the window is still rendered and discarded, because the engine's state at a moment is the whole set up to it; that cost is stated rather than hidden. Of the seven things §68 says such an object could drive, **all seven now do**: the waveform, the suggestions, the AI preparation, the autopilot, the replay, the practice rehearsal (§69) and the preview (§27) — §27's ghost is this same plan drawn over a record before anything is loaded |
| 69 | Practice lab | 🟡 | **Two records explored without altering the live master**, which is the sentence §69 rests on. `dj_app::practice` builds a set file for a mix that was never played and `replay` renders it headless — no fader moves, no deck is touched, and the record playing to the room keeps playing. **The real automix writes it**: it is stepped offline against a simulated playhead and the actions it emits *are* the rehearsal, so what you hear is what djmanzo would perform rather than a second implementation of a crossfade. Rehearsing an alternative style **does not restyle the held mix** — the transition is cloned — so the four files of one pair are §69's "hear alternative transitions" as things on disk. A rehearsal is cheap where a replay is not: it is synthetic, so it has no history to be faithful to and costs its own length whatever hour of the night it is. "Save successful transitions" is §24's *keep*, which ships. Comparing BPM, phrase structure and keys is the pair view's and is drawn there rather than a second time. What §69 lists and this does not have: experimenting with stems, testing FX and creating loops **live** — those need a second engine feeding the headphone output, which is real work and is not this. Nothing here can say whether any of it *sounds* right; there is no audio device in this container |
| 70 | Learning mode | ✅ | The coach ships |
| 71 | "What should I do next?" | ✅ | The assistant's next step is shown before it happens |
| 72 | User override matrix | ✅ | `dj_assistant::authority` — the directive's ten capabilities against six postures, verbatim and asserted against the directive's own table. The second of three gates the assistant passes, so it is what actually stops a mix rather than a diagram. Configurable both ways, except that Off and Watch cannot be widened |
| 73 | AI knows what is expensive | ✅ | `mistakes_are_costly` reaches the deck as careful mode |
| 74 | Contextual control rail | ✅ | **At hand**: `dj_app::at_hand` reads the snapshot for what the hands are doing — a platter touched, stems being played, a record against another, one cued and waiting — and answers with four to eight controls, each an action the parser already accepts. A latched one offers the other half, so "bass out" becomes "bass in" once the low band is out. The panel names the deck and why, because a row that silently became a different row is a row nobody trusts. Which deck is its own judgement: §41's focus fades after six seconds by design, so it follows the hands instead. §74's stem FX, tags, rating and transition points are named as absent — a rack of four numbers, and three things that belong to a record rather than a deck. **A control now has an identity, not only a label.** It was fifteen `Control::new` calls across five arms, so a control existed only as the string it happened to be built with and `bass in` and `bass out` were two different things; `at_hand::Reach` is the fifteen, each `Doing` names five of them, and one builder reads the deck for the label and the light. That is what gave §8's *preferred controls* something stable to name: a kept control is on the rail in every state, first, and marked on screen as the DJ's rather than djmanzo's judgement |
| 75 | Visual control of audio features | ✅ | **The row was empty because nobody had separated §75 from §25.** §25 is the inventory of what the waveform draws and §26 is what can be grabbed; §75 is the *rule* over both — *do not confuse visualization with control; every interactive visual needs clear semantics* — plus a list of nine audio properties. Three of its nine are exposed: **spectral balance** and **phrase boundaries** are drawn layers, and bass activity is readable in the spectral colouring. The other six — vocal density, drum density, breakdown, drop, energy, transient density — are behind the same wall as §25's eight unbuilt layers: the analysis does not exist, and they are named as absent rather than drawn empty. **The rule is enforced.** A vitest scans every waveform surface: an element stamped with a `data-layer` that takes a pointer must carry a role and a label, and one that takes no pointer must not wear a grab cursor and invite a drag that does nothing. **And §75's one genuine action ships.** The phrase boundary is the only audio property on the waveform a drag means something for — the analyser reads where the music starts again and is wrong often enough to be worth a handle, because a record with a four-beat pickup and one without look identical to a structure detector and put every marker a bar out. Dragging one sends `grid_phrase`, which snaps to the nearest beat, keeps the phrase length, and persists without marking the *grid* as the DJ's own: they corrected the phrase, not the beats, and marking the grid would stop a later re-analysis improving one nobody had complained about. Only the visible boundaries are handles — a ten-minute record has about seventy, and mounting all of them to show four would cost the whole record on every frame of a scroll. **And three of the six are exposed now.** *Energy*, *breakdown* and *drop* are `dj_analysis::energy::trajectory`, drawn on the whole-record view as columns, a wash and a line — the overview rather than the scrolling lane, for §27's reason: a breakdown two minutes ahead is off screen in a lane running at a few hundred frames per pixel, and *where does this record go* is a question asked before the record gets there. §75's rule holds over them: none takes a pointer, so none wears a grab cursor or invites a drag that does nothing. **Eight of the nine now, and the count in this sentence was wrong when it was written.** It said *four*, which was a slip: three were exposed at the start of the row and §75's own energy, breakdown and drop made six before a vocal was measured at all. *Vocal density* is `dj_analysis::presence`, and the reason beside it — *needs separation analysis nobody has written* — had stopped being true before it was written down: `dj_stems::hpss` separates a record with no model and no download, and what was missing was not the analysis but anyone asking it for a curve. It is drawn as a strip along the top of the overview rather than more columns from the floor, because the trajectory owns the floor and a second set of columns would read as a second opinion about *where does this record go* when it answers a different question. §75's rule holds over it: it takes no pointer, so it wears no grab cursor. **A window nobody measured draws nothing rather than zero** — absent and *nobody was singing* are different answers, and a strip that filled the gaps would claim the second when djmanzo only has the first. **And *drum density* with §25's `stems` layer**, from the same pass and the same masks: the percussive share of a moment is what a drum is, and it needed no second reading of anything. **And transient density ships, which makes nine of nine.** The reason filed against it — *a curve at a resolution the overview cannot show* — was wrong, and wrong in a way worth naming: it confused transients with their *density*. Individual strikes are indeed invisible where a whole record is six hundred pixels wide; a density is a **count over a window**, which is exactly what survives being drawn small. `dj_analysis::strikes` is peak-picking and nothing else — no pass over the audio at all. The onset curve the tempo estimator already builds is the measurement, and what was missing was a *reading* of it. **It is not the percussive share, and the two come apart constantly**: a sparse kick-and-clap pattern is high share and low density, a shaker under a pad is the other way round. The browser fixture is deliberately anti-correlated for that reason — its densest window is its quietest — so a component that drew the energy curve into the band fails rather than looking right. Absolute like the stem shares and unlike the energy curve, for the same reason: *where does this record go* is a question about the record and *how busy is this* is not. A window nobody measured draws nothing rather than zero. **Two numbers here are stated guesses and say so**: `strikes::BUSY`, the density the band saturates at, and `OVER`, how far above the local mean a peak has to stand — calibrated against synthesised percussion with a known pattern, because the only audio in this container is four seconds of test tone and a constant tuned until it looked right on that would be tuned to nothing. That calibration is weaker than real material: the pattern has clean transients and no reverb, and a record with a long tail on every hit would want a lower threshold. What is left for this row: nothing §75 lists |
| 76 | Library "AI lens" | 🟡 | **A toggle that adds, and can never replace.** §76 closes with "this must never replace the standard library view", and the shape keeps that true rather than promising it: `library_lens` is handed the ids the table is *already showing* and answers about those. It does not query, filter or order the collection, so turning it off leaves the standard view exactly as it was — because the lens was never inside it, and a browser test compares the standard headings with it on and off. Six of §76's eight columns ship, each from something djmanzo already knows: **likely next** from the same scorer the Next rail uses, **user affinity** from `dj_library::learned`, **phase suitability** from the record's own function tags against the night's phase, **transition risk** as the scorer's own bad news kept as named reasons rather than boiled to a number, **novelty and familiarity** from the play count and the last-played date, and **function tags**. A record cannot follow itself, so the row for what is playing has no next and no risk. A column djmanzo cannot answer is **blank, never zero** — an empty cell means no opinion and a zero means bad, and they are different answers. What §76 lists and this does not have: **crowd suitability**, which needs to know what the room is doing and so needs a camera or a microphone in it — named in the header rather than silently dropped. And novelty and familiarity are this DJ's own history, not the room's ears: the crowd's familiarity with a record is not something djmanzo can know |
| 77 | Exploration vs performance | ✅ | **Something switches on it now.** `Focus` had modelled the two modes since the cockpit was written, stored on every workspace — and, exactly like §78's `frozen`, nothing read it. What reads it is §18's attention budget, under a rule that is deliberately **one-way: a DJ's chosen focus may make the interface quieter and never louder**. The asymmetry is the whole design. Choosing a performing workspace is a DJ saying *keep out of my way tonight*, and that has to hold between records as well as during them — the machine has no standing to decide they have relaxed. Choosing a learning workspace is a much weaker statement, and if two decks are audible the room can hear them: an interface that got chattier on the strength of a menu choice made an hour earlier would be talking over a live mix. So the machine's reading of the night is the ceiling and `Attention::quieter_of` can only lower it — `min` on every count, `and` on the one flag, the lower of the two motion levels — which also means no focus can talk over §18's emergency. The focus reaches the 60 Hz pump through a shared cell rather than a file read per tick, seeded on the first workspace read so it survives a restart, and `get_snapshot` applies it too so a panel painting itself outside the pump cannot disagree with the next tick. §77's *graceful transition* is §18's and §31's existing machinery: the budget follows the night, the reflow gate stops anything moving mid-mix, and a test asserts the quiet mode is not a trap — the interface comes back when there is room |
| 78 | "Freeze" | ✅ | **The safety valve is connected.** `Workspace.frozen` existed, was stored, serialised and round-tripped through the resolver, and was read by *nothing at all* — which is worse than not having it, because a DJ who believes the layout is pinned stops watching it. It is now `Workspace.locked`, a set of §79's six, and Freeze is all of them. §78 lists four things it stops and djmanzo does all four, because it turned out to do exactly four things to its own presentation without being asked: **rearrange** (§17's phase promotion, the browser a layout opens for you, and the assistant's own `ui_request`), **resize** (§18's density bands following the window), **retheme** (§31's adaptation) and **restyle** (the six audio-reactive custom properties every control reads). The mapping from a lock to a freedom is one table in Rust, `Lock::stops`, and `Resolved` carries the answer so the interface never works out what a lock *means* — a judgement written in `App.svelte` is a judgement nobody can test. **The assistant's path is gated in Rust**, which is the half that could not be done in the interface: `ui_request` applies the arrangement, stores it, and only then tells the window, so a permit consulted in the shell would be consulted after the screen had already moved and the change had been written to disk. It beats every posture including autopilot — a valve with an override is not one. §78's fourth bullet, *no surprise visual transitions*, is the restyle lock plus §18's existing motion budget and ADR-0009's tier 0; nothing is duplicated |
| 79 | "Lock my workflow" | ✅ | **All six locks ship, separately, in Settings.** `cockpit::Lock` is §79's list verbatim — workspace, panel arrangement, control density, theme, waveform style, assistant layout behaviour — each with the sentence a DJ reads, listed by Rust so a seventh appears in the panel without anybody editing it. Locking one takes one thing away and a test asserts it takes *only* that one: a "lock the theme" that quietly froze everything would be Freeze under a narrower name, which a DJ would find out mid-set. §79's closing rule is the one that shaped the design — *the AI can still use the underlying state without rearranging presentation* — so nothing here stops the context engine reading the room, planning a transition or offering a record; `ui_request` is refused with a sentence that says a lock caused it, so a DJ whose assistant has gone quiet can tell whether to change the posture or clear a lock. **Three of the six stop the same behaviour**, and that is honest rather than a gap: workspace, panel arrangement and assistant layout behaviour are one thing with three names in today's djmanzo, because the only thing that rearranges panels *is* the assistant reading the night. **This row previously claimed a gap that is not one.** It said the waveform lock should also pin the layers a DJ chose. Nothing automatic changes the layers: they are written by the DJ's own picker and by the DJ pressing a §54 preset, and by nothing else. §78's four bullets are each about an *automatic* or *surprise* change, and §79's own sentence is that the AI may use the underlying state *without rearranging presentation* — so a lock stops djmanzo acting on its own, not the DJ acting deliberately. Building the claimed behaviour would have made a preset refuse a change the DJ pressed, which breaks the contract §7 and §54 both state: a preset is a starting point and everything it touches stays changeable. The lock already stops the one automatic thing that touches the waveform's appearance — §75's answering of the audio — so this row is complete as it stands |
| 80 | Persona learning | ✅ | **What was missing was not the learning — it was the disagreeing.** §13's `Tendency` and §81's `Profile` both ship and both are constructor-enforced, so neither can generalise from one surprising night, and both already produce sentences. What neither did is let a DJ say *no*. That is half of what §80 asks for, and it is the half that makes the rest safe: a learned claim a DJ cannot reject is one djmanzo may go on acting on after being told it is wrong. `persona::Verdict` is the answer the DJ owns, and a **rejected** claim is neither acted on nor raised again — both, because a DJ asked the same thing every week has not been heard and one whose *no* is recorded and then ignored has been lied to. **The register changes on purpose.** §13 writes observations — *"you often sweep the filter when the night is peaking"*, never *"you like filter sweeps"* — and §80 asks for the second kind of sentence, which is only honest with an answer attached. So the panel shows §80's own words, *Learned preference*, above each claim. **Three of §80's four, and the fourth says why.** Density, the assistant's posture and transition style are derivable from §81's profiles today. **And the fourth, which was mis-diagnosed at first.** The earlier row said djmanzo could not tell which stem a DJ reached for. That was wrong: `DeckAction::Stem` carries the stem and always has. What collapses all four into one is §14's *gesture* vocabulary — *six EQ verbs are one gesture a DJ would name* — and that rule is right for §14 and wrong for §80, because reaching for the vocal and reaching for the drums are one gesture to §14 and two different DJs here. So the coarse gesture stays coarse and `persona::stems_for` reads the actions. *Mostly* is §80's own word, so it is more than half rather than a plurality — a stem that is the largest of four at thirty per cent is not mostly anything — and never from fewer than twelve moves, three per stem, because a claim built on three gestures is what every learning type here refuses. **A claim needs every kind of night to agree.** A persona statement is an average by construction, and §81 exists because averaging a wedding with a club night is worse than learning nothing, so a DJ who runs dense at clubs and sparse at weddings gets no claim rather than the more common one. What is left for this row: nothing this container can build — all four of §80's traits are claimable, answerable and answered |
| 81 | User profile by context | 🟡 | **Conditional profiles, never one universal one.** `dj_app::setting::Setting` is §81's own six — club, beach, wedding, latin, practice, open format — and it is **told, never inferred**: djmanzo reads the arc of a night from the music and is right to, but nothing in the signal says *wedding*, and guessing it would file a whole night's habits under the wrong name. A `nights` row per session carries the setting, which is the one thing nothing can derive. Genre weights are **derived** from `history ⋈ nights ⋈ tracks`; the other four of §81's five are read off the action log as the night goes, because the log does not outlive the run that made it. `dj_app::profile::Profile` has private fields and is constructible only through `profiles()` — §13's discipline at the scale of a whole night: nothing at all under three nights of a setting, and each field silent until half the nights that spoke agree. A gesture counts once per night, not once per press. The sentence is written in Rust and always names the setting and the evidence. Drawn in two places for two reasons: the picker sits in **The night**, beside the arc it is the other axis of; the profiles sit in the assistant beside §13's tendencies, because both are djmanzo saying what it worked out about this DJ. **And three of §81's five now act rather than only being known.** The genre weights reached the Next rail first (see §12); density and automation tolerance reach the cockpit and the assistant through `profile::fits`, and the two are deliberately not treated alike. **Density is presentation**: §78 names *no automatic surface resizing* as one of four freedoms and §79 gives it a lock of its own, so a DJ who does not want it has already said so in the place made for saying it — and with the lock off, three or more nights of what this DJ actually ran at is better evidence than one window height. It is applied unasked. **Posture is autonomy**, which has no lock because it has its own control, and §9's rule is that autonomy above confidence is unsafe. A profile is evidence about *past* nights and says nothing about tonight's certainty, so the rule is asymmetric on purpose: djmanzo may turn its own assistant **down** on a profile and may only ever *offer* to turn it up. Turning it down costs one press to undo; the other direction hands a machine the mix on three previous evenings' evidence. **It sets; it does not own** — the same contract §7's arrangements and §54's setups state: it fits once, when the night is named, and then leaves both alone, because a version that re-applied on every poll would spring back the instant a DJ moved the density themselves. A withheld fit **says it is withheld**, because an interface that declined to act and said nothing looks exactly like one with nothing to say. Driven under Xvfb against five real wedding nights in this container's own library: with the assistant on autopilot, naming the night a wedding put it back on suggest by itself. **And the transition style is acted on now.** It was one of two §81 fields *shown and consulted by nothing* — djmanzo could tell a DJ *at weddings you mostly fade* and then propose a blend, every time, forever. `plan::plan_as` takes it and `choose_style` uses it in **the one case where the music has left the choice open**: tempos and keys both agree, so everything that overlaps two records works and which of them to use is taste. A key clash still gets the echo and a tempo the deck cannot reach still gets the cut, because those are the music's answers and three previous evenings do not outvote the two records in front of the DJ. `plan::STYLE_IS_TASTE` is the list a preference is allowed to be, and it is a list rather than a `!=` so that the two exclusions can each say why: a **vocal drop** needs a vocal to keep and stems to keep it out of, and the planner sees two records' tempo, key and phrase — proposing it would be djmanzo promising a mix it cannot perform; an **echo** is the music's own answer to a key clash, so a DJ whose commonest style is Echo has mostly been mixing records whose keys fight rather than expressing a taste. It reaches all three places the same plan is seen — §22's rail estimate, §27's ghost overlay and the step the autopilot performs — because a machine mixing differently from what the screen just promised is the worst version of the drift that splitting them causes. An armed `Transition` **holds** the style it was planned with, so a replan reproduces the answer the DJ threw away rather than whatever the profile has become since. Driven under Xvfb with five wedding nights mixed by fading: naming the night a wedding turned every rail row whose keys agree into `32-beat fade` and left the two whose keys clash at `32-beat echo`, on the same screen. What §81 lists and this still does not act on: **technique preferences**, the last of the five that is shown and consulted by nothing — it needs an owner that knows what a preference there would change. A profile with no genred plays behind it is shown in the assistant and deliberately does **not** claim to have ranked anything — found by driving the application, where a wedding profile built from untagged nights had the rail announcing a ranking it had not touched |
| 82 | Performance metrics for the redesign | ✅ | Every claim in this work carries a measurement, and the ones that could not be measured say so |
| 83 | Implementation strategy | ✅ | Nine phases in `GUI-OVERHAUL.md` §21 |
| 84 | Test every step | ✅ | Clippy, the full Rust suite, svelte-check, vitest and Playwright green before each commit |
| 85 | Test muscle memory | ✅ | The golden deck order |
| 86 | Test the action bus | ✅ | Pre-existing and still green |
| 87 | Test state consistency | ✅ | **Asserted, and two of the seven origins turned out not to exist.** `put_on_deck` is djmanzo's one load funnel and `tests/load_origins.rs` now drives two roads into it — the browser's, which every panel reaches through `load_track`, and the bus's — and compares everything a load leaves behind: the deck's name, the waveform summary, the library row and the session log. A Rust test then guards the rule the section's second sentence states: **only the funnel may say what is on a deck, or tell the log that something was loaded.** That guard matters more than the comparison, because the way this breaks is not a redesign — it is somebody adding an origin and setting the deck's name themselves, two lines that look obviously correct, leaving a title with no waveform, no library row and nothing in the log. **A controller and the line protocol could not load at all**, because loading is deliberately outside the action vocabulary (ADR-0003: it carries an `Arc`). The vocabulary for it already existed in the one place a load has always had to be written down — `SessionEvent::to_line` writes `load deck 1 <track-id>` into every session file — so that is the line `perform` now carries out, live. One spelling, three readers: what a set file records, what the command palette offers (it used to refuse the line and now does not), and what the bus performs. **By id and never by path**, because a path would hand anything that can reach the bus — including a socket — a way to make djmanzo read an arbitrary file. **And the network was worse than incomplete.** It dispatched straight at the engine's bus, skipping every interception in `perform`: `deck 1 eject` ejected the audio and left the interface showing a record that was no longer on the deck, and `record on` was answered *accepted* having started nothing, because the engine cannot open a file. It was the one origin not going through the application's own entry point, which is exactly why it was the one whose state differed. `dj_net::Carry` is where an action goes now, required rather than optional so a caller cannot quietly reopen the hole, and djmanzo passes its own `perform`. The socket therefore also obeys §72's takeover and everything else on that path, for free. Driven under Xvfb: an eject over the port cleared deck 1's name, BPM, key and waveform while deck 2 kept its record, and `load deck 1 <id>` over the port put a record on deck 1 with its analysis. That last verb needed a second fix that **only driving it found**: `Carry` took an `Action`, so dj-net parsed the line before the host saw it and refused at its own door the one verb §87 exists to make available. Every test on both sides of that seam was asking about actions, so none of them could see a verb that is not one. `Carry::carry_line` is the fix. §87 names seven origins; djmanzo has no *preset* that loads a record distinct from a setlist, and a setlist load is the assistant's path |
| 88 | Test adaptation | ✅ | `density.spec.ts` — the deck fits at every window tall enough, and a shorter window never gets a looser interface |
| 89 | Visual regression | ✅ | **All ten of §89's configurations are captured, and the layout rules are held in every one.** `budget.spec.ts` measures the arrangement djmanzo opens at — it exists because the controls a DJ mixes with drifted below the fold twice, found by a human with a screenshot both times. §89's contribution is that one arrangement is not coverage, so the same rules now run against Classic 2 deck, Pro 2 deck, 4 deck, Compact laptop, Preparation, Practice, Autopilot, Club, Watershed and High Contrast: every performing control on the first screen, every deck drawn and inside the window, every surface the workspace names actually there, no sideways scroll, and nothing thrown. **Pixels are deliberately not diffed**: CI installs its own Chromium on `ubuntu-latest` and this container has a different build, so font rasterisation differs and a baseline captured in either place fails in the other for reasons that are nothing to do with djmanzo — a suite re-blessed every run has stopped being a test. What is regressed is geometry, which fails for the right reason and does not care which machine drew the font. **Finding the four-deck configuration found a real defect**: the workspace carried a `decks` field, `toggleSurface` wrote it, and nothing read it back — and the write used the stored count rather than the live one, so the toggle never reached the file. A DJ who set up four decks found two next time, with the saved workspace still claiming four. Both halves fixed and verified in the running application. **And appearance is compared now**, which is what this row used to name as the gap in exactly those words. Not by diffing pixels — the reason above stands, and a baseline re-blessed every run has stopped being a test — but by reading what the browser **resolves**. `getComputedStyle` turns `var()` and `color-mix()` into numbers, and those numbers are identical on any machine, so appearance can be regressed exactly as long as what is regressed is the design language's own relationships rather than a bitmap. **The gap it closes is structural rather than cosmetic.** §30's roles are not in a theme package: a package carries fourteen base colours and the roles are *derived* from them in `app.css`, so whether two roles stay distinguishable is a property of a derivation meeting a palette, and it can collapse in one palette while holding in the other thirty-three. Nothing could see that — Rust checks the pairs, vitest checks that components ask for tokens, and `roles.spec.ts` checked one pair on one theme. Every must-differ pair is now checked in every shipped palette, light and dark, against a perceptual floor of 0.05 in OKLab; the surface ladder must stay a ladder; and the ordinary and dim text are held to 4.5:1 and 3:1 **as rendered**, because a component can ask for the right token and a palette can still ship a pair nobody looked at together. Which pairs matter stays Rust's judgement, read from a golden `cockpit::Role` blesses, so a pair added there fails here until every palette honours it. **The first run found one**: `pkg-daylight` in light drew its page and its panels in exactly the same white — 0.0000 apart — so every surface boundary in that theme rested on a hairline border, in the one palette meant for an afternoon set outdoors, where a bright screen washes a hairline out first. Every other light palette sets the page off-white and the panel pure white; this one was the outlier. The floors are ratchets with their real margins written down beside them: the closest pair djmanzo ships is `danger` against `success` in pkg-latin light at 0.0563, the closest surface step is pkg-stemlab light at 0.0096, and the worst text is pkg-industrial light's dim at 5.98:1 — a test prints all three so the numbers in the file can be checked rather than taken. What §89 still does not do: judge whether a palette is *good*. It can say two things are told apart and that text is readable; it cannot say a theme is pleasant, and nothing here should pretend to |
| 90 | Performance regression | 🟡 | **§90's last line is the deliverable; its five measurements are how you would notice.** djmanzo measures four of the five and shows them — the Mission Bar carries the master bus's CPU and dropout count, `report_bench` logs the interface's frame timing, and §11's health context now carries **worker utilization** for both background threads — and only one is ratcheted, which is the difference between measuring and regressing. `regress::ALL` is the five, each saying where djmanzo takes the reading and why it is or is not ratcheted; **measured and not ratcheted is a real place to be**, and four of the five sit there, because a number nobody can hold down is still a number somebody can read while a reading that is absent is one nobody can even argue with. One row is still neither, and says so. **Three cannot be ratcheted here and the reason is the same one §89 already records**: a frame rate, a memory figure and a CPU load are properties of the machine at least as much as of the code, CI installs its own browser and this container has no GPU, so a baseline captured in either place fails in the other for reasons no change caused. An xrun count is the fourth — measured against a null device it is always nought, so a ratchet on it would pass for the wrong reason. **What is machine-independent is allocation counts**, and that is where the one real ratchet went. `dj-engine/tests/rt_safety.rs` already proves the audio callback allocates zero times over thousands of blocks — the engine half of *do not let visually sophisticated changes compromise realtime audio*. The other half is the 60 Hz snapshot, which is where visually sophisticated changes actually arrive: built sixty times a second on the thread that also serves the engine's controls, one harmless-looking field at a time. `SNAPSHOT_ALLOCATIONS` holds it at 56 against a real 52 over six decks, and the headroom is that tight for a measured reason — a plausible per-deck field costs thirteen, and an earlier draft with thirteen of slack let exactly that mutation through. A second test fails when the slack grows past an eighth, because a ratchet far above what the code does holds nothing. **And the fifth measurement now exists.** *Worker utilization* had read "not measured" for as long as the row had: the background threads reported whether they were *working*, which is a light, and a light cannot answer *how hard* — a worker that flickers busy once a minute and one pegged for an hour both show it. `workers::Worker` is each thread's account of its own time, and both threads keep one: the 60 Hz interface builder and the library worker that decodes and analyses imports. **Share of its own loop, not share of a core**, which is what makes it worth having here at all — a CPU percentage depends on how many cores there are and what else is running, and this is a fact about the thread's own `while`. **Two numbers, never summed, because they mean opposite things**: high on the library worker is healthy (a queue is being got through) and high on the interface builder is §90's own last sentence arriving — *visual sophistication taxing the machine*, one harmless-looking field at a time. A single "busiest worker" figure would alarm during an import and say nothing about the thing §90 worries about. Absent is not idle: `None` is *no such thread*, and a build with no library open has no library worker, which is a different piece of news from one that is idle. Neither is ratcheted and the row says why — one of them is *meant* to go up. Driven under Xvfb, the health row reads `3% CPU · 0 dropouts · interface 1% · library 6%`. What is left for this row: **memory**, and that stays a refusal rather than a gap — a resident-set figure on a container sharing a page cache with a build is noise, and the allocation ratchet above is the part of it that means the same thing on two machines |
| 91 | Do not overengineer prematurely | ⚖️ | |
| 92 | Do not migrate technology | ⚖️ | Still Svelte 5, TypeScript, Tauri 2, the action bus, the ParameterRegistry, the Rust waveform |
| 93 | Show exactly what matters next | ⚖️ | The governing principle, and what the pinning work served |
| 94 | Promote and compress; never hide unpredictably | ⚖️ | Density compresses; nothing is hidden. The deck's own map is unchanged |
| 95 | The assistant is a second DJ, not a chatbot | ⚖️ | |
| 96 | "Listening to the room with me" | ⚖️ | Confidence is explicit where the room is read |
| 97 | Theme is atmosphere; semantics are sacred | ⚖️ | The token whitelist enforces the boundary in code |
| 98 | A whole night without hunting through menus | ✅ | Every performing control is one screen away at 1280×800 and a test says so; §74's contextual rail offers what the hands are doing; and the command palette reaches **the whole vocabulary by typing it**, including every verb that takes an argument and, since §87, `load deck 1 <track-id>` — so a record can be put on a deck without opening the browser at all. §58's ranking is what finishes it: the twelve entries a palette can show are now the twelve *nearest the hands* rather than the twelve that happened to be generated first. §98's five ways are each real — visible (one screen), near (the rail), one gesture (a panel button), one shortcut (Ctrl+K, and the keyboard reaches every handle §26 added), one contextual reveal (the lens, the room chip, the at-hand rail) |
| 99 | Analysis before code | ✅ | 24 sections and 700 lines, written before any `.svelte` file was touched |
| 100 | Use existing functionality aggressively | ✅ | The audit's own finding: this is mostly an integration problem, exactly as this section predicts |
| 101 | The feature set is a hidden engine | ✅ | |
| 102 | Final output from this task | ✅ | `GUI-OVERHAUL.md`, and this file |
| 103 | Success criteria | 🟡 | See below — nine of thirteen met. The table had ten rows and §103 names thirteen; *audience awareness*, *performance* and *extensibility* had never been judged at all |
| 104 | Coding style | ⚖️ | |
| 105 | Final instruction | ⚖️ | |
| 106 | Hardware-free testing of the senses | 🟡 | **The first thing it found is that neither desktop could have opened a camera.** On Linux, WebKitGTK asks its host before opening a device and a request nobody answers is denied; Tauri answers it on macOS and not on Linux, so a DJ with a working webcam was told djmanzo was not allowed and sent to privacy settings Linux does not have. Reproduced in the shipped webview under Xvfb with WebKitGTK's own mock camera and microphone (`DJMANZO_MOCK_CAPTURE`), fixed in `dj_app::senses`, and the same build then read *light 6%, movement 1%, loudness 62%* end to end through `room_saw`. On macOS the bundle had **no `Info.plist`**, and the OS will not open either device for an application that does not say why; the two sentences are now there and a Rust test holds the file to them. A camera or microphone is granted only to djmanzo's own page (`senses::is_own_page`, with the lookalike addresses tested). For Chromium, `ui/e2e/senses/media.ts` draws the stand-ins from a seed at test time — a dark club at its peak, a room that has emptied, a beach party in daylight, a pushed booth microphone, low chatter and a hummed tune — because the openly licensed festival archives are refused by this network and footage from a streaming site could not be a fixture. **What they showed, now fixed and held by `ui/e2e/senses.spec.ts`:** movement aliased with the tempo — a floor of seventeen people dancing at 120 BPM read 0.000, because a two-second look is exactly four beats — and changed scale with §48's tier (0.24 at two seconds, 0.10 at eight); it is now two frames a sixth of a second apart at a moment moved randomly within each period, and reads 0.07 and 0.17/0.17/0.16. Loudness was a 43 ms window read through a microphone left in its call-tuned defaults, so a room of chatter made at -42 dBFS read 0.21 and one loud mix read anywhere from 0.65 to 0.80; it is now two thirds of a second with gain control, noise suppression and echo cancellation asked off, and reads each file's own level to within 0.01, steadily. A machine with a microphone and no camera read nothing and was told it had neither; it now reads loudness. Closing the surface while it is looking is held to let both devices go. Each fix was mutation-tested; the one that survived — the hum's microphone settings — was reverted rather than kept, and the every-eight-seconds test is recorded as a guard rather than proof, because on one run the old algorithm passed it. What is not here yet: the other hardware djmanzo reads that this container has none of — controllers and timecode vinyl — through a stand-in of the same kind. None of this says anything about how a real room reads — only that the path runs and which way the numbers move |
| 107 | Karaoke, researched and improved | 🟡 | **Audited first, and the audit was the finding:** `docs/KARAOKE.md` described a karaoke feature and almost none of it reached a DJ — the band-limited centre cancel existed as tested DSP that nothing in the engine called, synced lyrics were fetched from LRCLIB and used only to search the collection by words, and there was no singer queue and no singers' screen. **Researched** (sources and what each contributed in `docs/RESEARCH.md`; feature descriptions only, OpenKJ noted as GPL and not read): hosts' rotation rules, per-singer key memory, a vocal guide, break music, a words-only second screen, word-level Enhanced LRC. **Built so far — the rotation**, because a karaoke night is run by it: `dj_app::karaoke` holds the rules the hosts write down (first come first served; one song a turn and then the back; not ready when called is the bottom with songs kept; a singer's key remembered with their song and offered when they ask again, across nights), the *Singers* surface draws it with the up-next singer large, a song picked from the collection as it is typed, and *Load on N* putting the song on a deck **and then** its key, and the *Karaoke* activity (F8) opens it beside the decks. Mutation-tested: a singer who keeps their place after singing fails two Rust tests, and a key sent before the record arrives fails the browser test. **The vocal, taken out — built.** One knob per deck, `deck N voice <0..1>` (1 as recorded, a quarter the host's guide vocal at about −12 dB, 0 out), two mechanisms behind it: where the record is separated it is a gain on the vocal stem *over* the DJ's own level for it; where it is not yet — the first seconds of a record loaded a moment ago, or a machine with no separation — `dj_dsp::karaoke`'s band-limited centre cancel runs on the whole mix at the depth the knob leaves out, so the kick and bass survive. In the deck's render path, allocated with the deck, no locks; published as `DeckParam::Voice`. The Singers surface has *As recorded · Guide · Out* per deck, and the deck itself carries a *Voice out* / *Guide vocal* chip whenever it is not as recorded — the setting outlives the karaoke screen, and a DJ back in the mix must not play a record with its vocal missing and not know why; one press on the chip puts it back. The assistant is refused it (no capability covers it). Held by two engine tests (the stem path: out leaves exactly the other nine tenths, a guide a quarter of the voice, and a DJ's own half level for the vocal stem is kept under it; the fallback: a centred 1 kHz tone under a tenth of itself at voice 0, a centred 60 Hz tone above 85 %) and a browser test (the switch sends the action, the chip appears from the snapshot without moving the waveform, and puts the voice back); each mutation-tested. Driven in the running application: *Out* on deck 1 came back from the engine and the chip appeared. **Nothing here can say how it sounds** — this container has no speakers. **The singers' screen — built.** The synced words LRCLIB returns were stored and never shown; `dj_library::lrc` now reads them — LRC, several stamps on one line, Enhanced LRC's word stamps, the `[offset:]` tag — lenient where a file is untidy and strict where it would be wrong (an untimed line is dropped, not guessed). `singer_lyrics` sends them for a deck's record from the library, so the screen never waits on the network; untimed words are sent still, because a wipe at a guessed pace leads a singer wrong. A *Singers* panel opens on a display of its own from the Singers surface: the line being sung, large, wiped word by word where the file times words and evenly otherwise; the next line under it; a bar's count-in before the first line and after any break; and the next singer's name. It follows the deck the singer is on — the one playing with its vocal out — not merely whichever is playing. Held by six parser tests, two DTO tests, eight `lyricsAt` unit tests and four browser tests on the detached panel; mutation-tested (a screen that followed the first playing deck, and one that filled whole words only, each fail). Driven in the running application with a timed lyric stored for a synthetic record: the line wiped in the voice's own colour with the next one under it. **Not yet:** break music between singers |
| 108 | Social networks | 🟡 | **Audited first.** What existed: A6's *share a set* — a night's tracklist handed to WhatsApp, djmanzo preparing and the DJ pressing send — and the request page behind a QR code. What a DJ's social presence mostly is now is a **live stream**, and nothing reached it. **Going live** (`dj_app::live`, `dj_net::overlay`): the record on the stream, in both ways OBS carries text — a transparent overlay page for a Browser source, served on `127.0.0.1:7332` only (nothing in the room needs it, and its handler holds one string: no book, no bus, no deck), and `now-playing.txt` for a Text source set to *Read from file*, written whole and renamed over so it is never half a title, written the moment the switch goes on (OBS does not notice a file that appears later) and emptied when it goes off. It names **the record the room hears most** — channel fader times the crossfader's constant-power gain on its side, a cue in the headphones never — and changes hands only when another is half as loud again (`TAKES_OVER`, about 3.5 dB), so a blend moves the name once rather than flicking it where the two ride level. Off until switched on in Settings → *Going live*, which shows both addresses with copy buttons and what the stream is being told. Held by four `live` tests (loudest heard deck, one change per blend under a hand's wobble, a deck's level, the file written whole), four `overlay` tests over a real socket (the words and their change, nothing playing says nothing, a title cannot become markup, two read-only paths and nothing else) and `e2e/live.spec.ts`; mutation-tested — no margin, a paused deck heard, any path answered, a title unescaped, a cached answer, and a switch that sends nothing each fail. **Driven in the running application:** the file and `/now.txt` said the playing record; the crossfader hard right named deck 2, the centre kept it, hard left named deck 1; an open overlay page followed without reloading, and went quiet when the only heard deck ran out. Research and sources in `docs/RESEARCH.md`. **Handing a night to the networks:** A6's share sheet now offers **X, Bluesky and Threads** beside WhatsApp, under the same rule — each network's documented compose address with the text in it, the DJ reading, editing and posting (`dj_app::share::Channel`). What fits is each channel's own: the link under what a URL handler takes, and the post under the network's limit, measured never shorter than the network measures it (X's weighted count, where an emoji or a CJK character is two; characters for Bluesky, never fewer than its graphemes); the preview and the warning are the chosen channel's (*5 of 12 won't fit in a 280-character X post*), and the message says how many more. Telegram is left out because its share link requires a link to share and a tracklist has none; Instagram and TikTok have no compose address for text. `share_channels` gives the panel its list, `share_to` builds the address in Rust from the channel's word. Held by three new `share` tests — every channel's composer, X's weights, and a sweep of set sizes *and* heading lengths on every channel measured independently of `fits`, which the tail-trimming mutant first slipped past until the headings were swept — the `share.json` golden of what each channel carries for one fixed night, and `e2e/share.spec.ts`; mutation-tested (X unlimited, X weighing everything one, the tail note unchecked, a button that always hands to WhatsApp). Seen in the running application: the four channels, X chosen, *Open X*. **A recorded mix's chapters** (`dj_app::chapters`): the share sheet of a night lists every recording made during it — named `set-<unix start>.wav`, its length read from its own header and bytes (`wav::Wav::seconds_of`, so one still being written is measured by what it holds) — each with the night's tracklist timed against the file, as a YouTube description's chapters, which Mixcloud's tracklist takes too: the record already playing when recording started is `0:00` if it came in within ten minutes; a first record within thirty seconds of the start is moved to `0:00`, a later one leaves `0:00` as *Start*; a record that lasted under ten seconds before the next was a preview and is not a chapter; the sheet says when YouTube will not draw them (fewer than three). **Plays are now stamped when a record came in**, not when it crossed the thirty seconds that make it count (`PlayWatcher::heard`): in the running application the demo's records were stamped eleven seconds after launch, when they started, where the old stamp would have landed after recording began and made the first chapter wrong. Driving it found a bug the tests had not: a recording started after the night's last *counted* play was skipped by a time comparison, though that record was still playing — the scan now asks the chapters instead (`chapters::in_folder`), and a folder test with a recording started after the play holds it (mutation-tested: the old comparison fails it). Held by seven `chapters` tests and one `play_tests` test, and `e2e/share.spec.ts`'s recording test; mutation-tested (the record playing at the start ignored, previews kept, a late first record moved to `0:00`, wall-clock times). Seen in the running application: *Chapters for set-1790239988.wav (0:54)*, `0:00 … synthetic-124`, and the note that YouTube needs three |
| 109 | Activity mode | 🟡 | **A second way of working the cockpit, organised by the job rather than the style.** §7's twenty-three workspaces are Club, Wedding, Latin, Laptop — a menu to choose from before a set. `dj_app::activity` ships seven organised by what the DJ is doing this minute — **Dig, Mix, Perform, Prepare, Requests, Autopilot, Practice** — each carrying a workspace and nothing else of its own, so choosing one applies it through the path every arrangement already takes and pinned panels, locks and §5B's deck compositions behave as they always did. **Activity mode** puts the strip where the row of panel buttons stood and hides the stage pickers; REC, Mark and SAFE stay. **Switching fits a live set:** F1–F9 reach the activities and the key under Escape goes back — not the digits, which are the default keyboard's hot cues, and a Rust test holds that the keys stay free in it; the keys act only in the mode and never while typing; F5 no longer reloads the webview mid-set (checked in the shipped webview); and **the decks do not move**, entering the mode or between any two activities — a browser test measures it, and found the first version moving them 27 px because applying an activity's density rescaled the interface. Density is now the DJ's, not the activity's. **A DJ's own:** arrange the full cockpit, keep it by name, and it takes the next key; a shipped name is refused and said. **§115's assistant suggests and never switches:** `activity::suggest` marks one tab with a reason — ninety seconds left and nothing loaded is *Dig*, the next record loaded is *Mix*, two audible is *Mix*, the automix driving is *Autopilot*, requests waiting on a steady record is *Requests* — and a test waits for the next poll to prove the screen did not move. **Found on the way:** the Requests activity placed a surface that was declared and never drawn, and opened as the collection alone; `requests` is now drawn beside the decks with *Find it* reaching the collection's search, and every activity and preset is checked against what the shell draws rather than what it declares. What is not here: switching from a controller (the `ui` operations are not in the controller vocabulary), and a karaoke host's activity, which waits for §107 to give it a surface of its own **Found by driving it: a restart changed the size.** An activity applied its deck composition through the DJ's own layout choice, and the next start restored that layout with its density, so a host who closed djmanzo on F8 reopened it drawn larger. An activity's composition is no longer recorded as the DJ's layout; a restart in activity mode puts the activity back itself, at the DJ's size. Two browser tests hold it (no layout choice is written by switching through every activity; a restart in activity mode re-applies it and keeps the scale), each mutation-tested, and the real application reopened at the same size. |
| 110 | Spectral colour on the waveform | ✅ | **Every pitch its own colour of light, and the default.** Reworked after the owner's clarification (the first version added eight bands to white; the owner: *white is out of the picture as we never got all frequencies at once*). `dj_render::wavelength_for` maps hearing's ten octaves onto sight's one, octave for octave — `λ = 750 · (380/750)^p` — so 20 Hz is a red at the edge of infrared and 20 kHz a violet at the edge of ultraviolet; `wavelength_rgb` (Bruton's piecewise spectrum) puts those hues on screen and dims the ends as the eye does, into a 256-step table. Each column is §110's eight bands (`SPECTRUM_EDGES_HZ`) stacked from the centre out, lowest at the heart, each as thick as it sounds in decibels (`STACK_RANGE_DB`) and coloured across its own pitches, so a column is one continuous spectrum stretched by what is loud; full brightness inside the RMS body, dimmer beyond it. **Live:** the lane draws each tile as its low, mid and high parts (`EqPart`, split at the band edges nearest the isolator's crossovers) plus a grid layer, and `ui/src/eqLight.ts` sets each part's opacity from its EQ knob and the filter's corner — a killed band is a ghost where it stood, a compositor opacity with nothing redrawn. Measured off the load path (the colour lands a moment later under a new epoch); the light theme draws the same hues as ink; the EQ-matched three bands stay one choice away. Held by `dj_render::tile::light` (20 Hz deep red and 20 kHz violet, landmarks by instrument, hue monotonic in pitch, **nothing white** in the table or in a full-spectrum column, two tones two colours in their places, decibel rooms, parts partition the column, grid layer alone, UI crossovers read from the TS file) and `e2e/colouring.spec.ts` (parts and their opacities from the knobs, grid never dimmed, unsplit while pending); each mutation-tested. Seeing instruments coming, rhythm and the song's evolution in the waveform's background are §116. |
| 111 | Legal music sources for karaoke stems | 🟡 | **Researched first, and the answer decides what is built** (sources in `docs/RESEARCH.md`): streaming that may be stemmed live exists only inside partnerships — Beatport in its partner applications, TIDAL only with its paid DJ Extension, Apple Music and Spotify never — and none offers a public streaming API, so streaming stays `dj_sources::partner`'s *ready, waiting on an agreement*. A **bought download** is the DJ's file to play and to take the voice out of (§107's voice knob); a room needs the venue's performance licence, and lyrics on a karaoke screen the venue's licence for those. **Built:** `dj_sources::stores` — Bandcamp, Beatport (Beatsource's catalogue now lives there), Traxsource, Amazon, Apple, and for karaoke Karaoke Version (licensed backing tracks) and ccMixter (Creative Commons a cappellas) — only search addresses that could be confirmed are searches, the rest open the store and say *search there*. *Find it to buy* appears where a DJ meets a song they do not have: a collection search that finds nothing, and a singer's request nobody has (karaoke's stores first). `open_store` builds the address in Rust from the store's table and the encoded words; the webview never hands over a URL. The sentence about what buying covers is always beside the links. **The downloads folder:** `dj_app::downloads` watches the folder the DJ chooses, waits until a file stops growing (never a `.part`), reads its tags, and files it into the chosen music folder under its **family** — `dj_core::genre::family_for`, so *Deep House* and *house* are one folder and *Drum & Bass* and *dnb* another — and its artist; backing tracks go to `Karaoke/`, a cappellas to `A cappellas/`, an unknown genre keeps its own spelling, no genre is `Unsorted/`, no tag can place a file outside the music folder, and nothing is overwritten. Then it scans the new folder into the library. Off until the DJ switches it on; the two folders must differ. Held by three store tests, five filing tests, a golden of `store_links`, and three browser tests; each mutation-tested (genre spelling instead of family, answering a file on first sight, overwriting, and a link sending the wrong words each fail). **Driven in the running application:** two tagged WAVs dropped into the watched folder were filed as `House/Kerri Chandler/rising.wav` and `Karaoke/Queen/sing-along.wav` and appeared in the library with their tags. **Not yet:** album downloads that arrive as `.zip`, and a genre guessed from the audio when the file has no tag |
| 112 | Watershed, improved or hidden | ✅ | **Hidden for now, which is the branch the owner named.** Driven in the running application first: open, it is a band of about 240 px above the decks that restates what the waveforms and the mixer already say — two streams merging for the crossfader, a lane per deck, *keys clash* in words — and on a 1280×800 screen it pushed a deck's pads off the bottom. Making it *much much better* is a redesign in competition with nine other asks, so it came off the main bar and stayed what §55 says it is: a **world**, opened by choosing *Watershed Living* in the theme picker and closed by an × on its own band, where the DJ who opened it is looking. Nothing is deleted, which is §55's *do not delete the existing watershed concept*. Both halves tested in the browser — no switch on the bar, and the band's own close control closes it (mutation-tested: a close that does nothing fails). What would earn it the bar back is information it alone carries; §110's spectral colour, drawn as the colour of the water, is the candidate |
| 113 | More themes, functional colour | 🟡 | **Functional colour first, because it is what the themes are built on.** `app.css` now carries tokens that mean a function rather than a mood: `--band-low`, `--band-mid`, `--band-high` — the EQ bands in the colours §110 draws their frequencies in on the waveform (the reds of 20–266 Hz, the greens of the voice, the violets of the air), the same three hues in every palette because they are the spectrum's, only their lightness following the ground — `--knob-value`, which the knob geometry's value arc now reads, so a control can say what its arc means, and `--level-gradient`, calm to warning to clip. Applied: each EQ knob wears its band's colour, so the LOW knob is the colour of the heart of the waveform it takes away; the filter knob wears the colour of the side it keeps (a low-pass the lows' red, a high-pass the highs' violet); the deck's level meter draws the gradient across the whole meter and uncovers it from the right, so a quiet signal is only ever the calm colour — still a compositor scale. **Three more themes, designed from what a colour does** (twenty in all): *Spectrum* (a neutral graphite so the spectrum is the only colour), *Signal* (a booth colour code: green going, cyan chosen, amber about to, red stop) and *Aurora* (a teal-to-violet night sky, the two ends of every gradient). Each is a palette in both modes, a package and a row in `dj_app::theme`; the contrast tests hold them, and `cockpit`'s must-differ guard caught Spectrum's first mint-less draft, whose amber warning and orange second accent — the drums' and the other stem's colours — were 17 apart. Held by `e2e/functional.spec.ts` (the three EQ arcs are the three band tokens and differ, the filter follows its side, the meter's cover is 1 − level over a gradient, the three themes are worn from the switcher); mutation-tested (arcs back on the accent, a cover that grows with the level). Seen in the running application: HI violet, MID green, a killed LOW with no arc. The pads by what they do, and the crossfader's two sides, are §114's |
| 114 | Shapes of controls | 🟡 | **A control shaped like what it does**, three ideas each replacing a number a DJ had to read. **A tone knob draws the curve it puts on the record:** the HI knob's face is a high shelf, the MID's a bell, the LOW's a low shelf and the filter's the slope it cuts, each at its setting on hearing's 20 Hz – 20 kHz axis (`ui/src/controls/faces.ts`). The curves are the DSP's own arithmetic — `dj_dsp::eq`'s Linkwitz–Riley bands, whose outputs are in phase so a band's share is a real weight, and the sweep filter's Butterworth, 3 dB down at its corner and 12 dB an octave past it — and `dj_dsp::eq::tests::the_knob_faces_draw_these_filters` reads the crossovers, dead zone and corners out of the TS file (the sweep's two ends became `SweepFilter::LOW_PASS_FLOOR_HZ` and `HIGH_PASS_CEILING_HZ` to be read). **A control that rests in the middle fills from the middle:** the filter from its centre, an EQ from unity, the pitch fader from zero, each with a detent mark; filled from the bottom, each read as half on when it was untouched. **The transport pads light in what they do**, the hardware's own code — PLAY in `--success`, CUE in `--warn`, SYNC in `--selected`, a tint so the label stays legible — and EJECT, the press that cannot be undone, is ringed in `--danger` at rest (§113's *pads by what they do*). **Natural forms where the theme is a natural one:** the eight organic packages, the default among them, use `GeometryStone` — each knob a river stone grown from its name (three low harmonics, held between the value track and the box, so the LOW knob is the same stone on both decks and every night), each pad a superellipse pebble filled in its own outline, each fader slot a reed; the value is still drawn on a true circle over the stone. Held by `faces.test.ts` (sixteen: bands share the signal, flat at unity, a kill a floor only in its own octaves, the Butterworth's corner and slope, fills from the rest in opposite directions, pads by role, stones individual and bounded) and `e2e/shapes.spec.ts` (five, on the deck); mutation-tested — a face ignoring the value, rests ignored, PLAY in the accent, stones drawn as circles, every knob the same stone, and a drifted crossover each fail. Seen in the running application (WebKitGTK): a killed LOW drawn as a floor on the left, a 60 % low-pass as an arc from the centre and a falling slope, PAUSE lit, EJECT ringed. **The crossfader draws the law it mixes by** (`ui/src/crossfader.ts`, `dj_dsp::crossfader_gains`' constant-power curve, the only one the engine uses): under its track, side 1 falling and side 2 rising across the same throw as the thumb, both at 71 % in the middle, with a point where the thumb stands on each and a cut side's number dimmed; the same words are the slider's `aria-valuetext`. Its cap is a fin, as a crossfader's is. The plot sits outside the slider, because inside it the slider's box grew and moved the crossfader's centre below a 720-pixel window (`regression.spec`'s compact laptop caught it). Found on the way: every master-strip slider mapped a drag across its whole box, which takes in the thumb overhanging the track, so a press at the track's end reached 0.94 of the throw; it is measured along the track now. Held by `crossfader.test.ts` and two more `shapes.spec` tests, each mutation-tested (a linear law, points that ignore the fader, the old mapping). **Not yet:** the jog wheel, and shapes for the controls outside the deck (sampler, FX, stems) |
| 115 | A GUI for a DJ's daily work | ⬜ | Asked 2026-09-24. Close to §3's thesis and §105's *exactly what I need, exactly when I need it*; carried by §109's activities, §113's themes and §114's shapes rather than built on its own |
| 116 | Seeing the music coming | 🟡 | **Frequency and intensity beyond amplitude** are §110's column: every pitch its colour, each band as thick as it sounds, so a kick is a red core, a voice a yellow-green band and hats a violet fringe, visible before they arrive. **What changes next:** `dj_analysis::energy::Trajectory::changes` reads the trajectory's windows into the moments a DJ plans around — a current coming in or going (the voice by §27's share rule, one answer with `voice_enters`; drums, bass and the rest by their level against their own busiest, because shares add to one and a bass arriving under a steady kick first read as *Drums out* in the running application), a stretch that builds or settles — with hysteresis and the breakdown detector's two-window hold. The lane names the next two with how far away, in bars then beats (`ui/src/ahead.ts`: *Breakdown · 8 bars*, *Vocals in · 10 bars*), a drop outranking a rise and a breakdown a settle at the same place, each under its §25 layer's switch; the record's energy is ground under the lane, stepping up where it builds; the overview marks every arrival (solid notch) and departure (hatched) in the fader colour of that current. Held by `energy::change_tests` (entry and exit, one window is a fill, no flicker, a kick under a new bass has not gone, builds from where they move), `ahead.test.ts` and `e2e/ahead.spec.ts`; mutation-tested. **The melody line:** `dj_analysis::melody::pitches` — the hum search's YIN reading before it is centred, so the notes keep their height — is read on the post-load thread beside §110's spectrum (about 0.7 s for five minutes, set just before the spectrum so the epoch the lane re-asks on finds it, and dropped if the deck has since been reloaded). `melody_line` sends each point with the step of §110's spectrum its pitch sits at, and the lane draws it as a line over the waveform, height by semitone and colour by that step, broken at rests and at leaps no tune makes in a tenth of a second (`ui/src/melody.ts`) — a new `melody` layer in the §25 table, sharing `Sound` because it is the record read as notes, and switchable because it is laid over the waveform rather than being it. Seen in the running application: a low note drawn orange and low, the sung line yellow and higher. **Not yet:** rhythm drawn in the lane beyond the beat grid and the overview's transient density |

## §103's thirteen success criteria, judged honestly

| Criterion | | |
|---|---|---|
| Human DJ usability — usable without learning the AI | ✅ | The AI has never been in the way; §4 was honoured from the start |
| Professional density — fast adjustment without opening panels | ✅ | Every performing control is on one screen at 1280×800 |
| Adaptability — simple for a beginner, dense for a professional | 🟡 | Density adapts; the layouts that would express the two ends do not |
| Modularity — a personal workflow can be constructed | ✅ | Surfaces dock and persist, §7's 23 arrangements can be chosen and then edited, and an edited one is now kept under a name of the DJ's own — listed apart from the shipped ones, remembered across restarts, and worn on the next open rather than reverting to the preset it was saved from |
| Predictability — adaptation never feels random | ✅ | Bands rather than a continuous ratio, so the interface settles; asserted by test |
| AI subtlety — everywhere useful, dominant nowhere | ✅ | |
| Instant takeover — human input always wins | ✅ | |
| Library quality — finding a track as strong as mixing it | 🟡 | All four of §20's views ship, and the performance table now carries fifteen columns with a picker rather than a fixed six. What keeps it short of ✅ is the six columns that are absent — and the reason beside the two that matter has now gone stale twice over: **both analyses exist**, and one of them has reached a column. Whether a record has vocals is `presence` (§25) and ships as §20's *vocal* column; where a record's breakdowns are is `energy::trajectory` (§75) and is drawn on the overview but is not a column, which is a different and smaller piece of work than the one this row used to describe |
| Visual utility — visualisations carry actionable information | 🟡 | Stale as ⬜ since §25: the waveform draws nineteen of the twenty layers, and eight of those answer a question rather than showing a shape — the phrase grid, the seam, the mix-out window, §27's ghost, the grid's own uncertainty and the runway among them. What keeps it short of ✅ is the **two** that are not drawn — and the list that used to stand here named five of them wrongly, which is the failure mode this file exists to prevent: *breakdowns*, *drops* and *the energy trajectory* had shipped with §75, and *vocal presence* with §25's eighteenth layer, while this sentence went on calling all four things nobody had written. Stem presence has since shipped too — and the guess written here about what it would need, *a separator running over a whole record*, was wrong in the same way: it needed the masks, not the audio. The one genuinely left is the **crowd**, which needs a room this container does not have |
| Theme semantics — colour communicates | ✅ | **Stale as 🟡 since §30.** All fourteen roles are colours the interface draws, each an alias onto a token the palettes already define so all seven packages get all fourteen; a Rust test reads `app.css` and fails on a role with no value, a must-differ pair collapsed onto one token, and a role nothing asks for. §33 then added the rule that keeps it honest in the other direction: colour is never the *only* channel for critical state |
| Audience awareness — room feedback helps without pretending to be omniscient | 🟡 | The *without pretending* half is the one that is finished, and it is the harder one: `Room::glance` reads the last twenty minutes and counts senses rather than averaging them, a room nothing is watching is given no direction at all, and the confidence is stated as `2 of 3 senses` rather than as a percentage djmanzo cannot support. What is missing is the feedback itself — this container has no camera and no microphone, so every sense is a reading the machine has never taken |
| Performance — audio is the highest-priority subsystem | ✅ | The audio thread never allocates, locks, does I/O or blocks, and a test asserts it; every server, decoder and analyser is on its own thread; the load and the dropout count are on §5's bar so a DJ sees the cost of anything they turn on. What this container cannot say is how it *sounds*, and nothing here claims to |
| Extensibility — new surfaces, themes, techniques and packs without rewriting the shell | ✅ | Each of the four is a table rather than a code path: `cockpit::surfaces()` and the shell's own `DRAWN` list (kept in step by a test), `themePackages`, `dj_assistant::technique`, and `posture::packs`. §41's `UiOp` vocabulary means a new surface is reachable from the palette and from the assistant without either learning about it |

## The count

Of the 116 sections: **64 done, 32 part, 1 open, 19 standing rules.**

Counted by a script over this table rather than by hand, and the first hand
count was wrong in all four columns — which is the argument for the script.
Re-run it against this file:

```
python3 - <<'EOF'
import pathlib, re
from collections import Counter
rows = re.findall(r'^\| (\d{1,3}) \| .*?\| (✅|🟡|⬜|⚖️) \|',
                  pathlib.Path("docs/DIRECTIVE-STATUS.md").read_text(), re.M)
print(len(rows), Counter(m for _, m in rows))
EOF
```

Standing rules are counted separately on purpose. Folding them into "done"
would inflate the number — a constraint honoured is not a feature delivered —
and they cannot be "open" either, since they are being obeyed. Excluding them,
**64 of 97 deliverable sections are complete and 32 more are partly there.** Of the original 105, nothing is untouched: every deliverable section has something real behind it, which is a different claim from every section being finished and is worth keeping apart from it. The 8 open rows are all among those asked for since.

That is the same state the phase view calls "about 40%", counted a different
way: 64 whole plus 32 halves over 97 is 82%, and the phase view is stricter
because a phase only closes when its gate is met. Neither number is wrong;
the phase view is the one to quote, because a gate is a fact and a half is a
judgement.

The done work is deliberately front-loaded. `GUI-OVERHAUL.md` §20 records that
everything downstream depended on the widget-tree renderer; that dependency is
discharged, which is why sections 6, 23, 59 and 60 could ship at all.
